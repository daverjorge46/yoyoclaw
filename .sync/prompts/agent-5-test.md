# Agent 5: QA/테스트 엔지니어 - 작업 지시서

## 너의 역할
OpenClaw의 테스트 커버리지를 확인하고, 가이드 문서의 코드 예제를 검증하는 QA 엔지니어.

## 프로젝트 경로
```
/Users/jeon-yeongjin/Desktop/💻 개발/1. GIT/03. 오픈클로/
```

## 브랜치
`improve/master-guide` (이미 체크아웃됨)

## 작업 목록

### 1. 가이드 문서 코드 예제 검증
- `연구자료/개선사항/openclaw-master-guide/` 전체 21개 문서에서 코드 블록 추출
- 특히 아래 문서에 주의 (텔레그램/AI비서 설정 예제 포함):
  - `04-telegram-assistant-guide.md` / `05-AI비서구축.md`
  - `07-부록.md` / `appendix-glossary.md`
- 각 코드 블록이 실제로 실행 가능한지 확인:
  - 파일 경로가 실제로 존재하는지
  - 명령어가 실제 CLI 명령과 일치하는지
  - 설정 예제의 키 이름이 실제 코드와 일치하는지
- 검증 결과를 문서별로 정리

### 2. 기존 테스트 상태 확인
- `pnpm test` 실행하여 현재 테스트 통과율 확인
- `vitest.config.ts` 및 관련 vitest 설정 확인
- 커버리지 리포트 생성 (가능하면)
```bash
pnpm test -- --reporter=verbose 2>&1 | tail -50
```

### 3. 트러블슈팅 가이드 이슈 검증
- `연구자료/개선사항/openclaw-master-guide/08-troubleshooting.md` 읽기
- 문서에 나열된 이슈들이 실제 코드에서 해당 에러를 발생시키는 경로 확인
- 해결 방법이 실제로 작동하는지 코드 수준에서 검증

### 4. 주요 모듈 테스트 커버리지 점검
테스트 파일이 존재하는지, 핵심 케이스를 커버하는지 확인:
- `src/gateway/auth.ts` ↔ `src/gateway/auth.test.ts`
- `src/agents/model-fallback.ts` ↔ `src/agents/model-fallback.test.ts`
- `src/agents/skills.ts` ↔ 관련 테스트들
- `src/commands/doctor.ts` ↔ 관련 테스트들
- `src/security/audit.ts` ↔ `src/security/audit.test.ts`
- `src/agents/context-window-guard.ts` ↔ 테스트

### 5. 테스트 갭 리포트 작성
- 테스트가 없는 핵심 모듈 목록
- 테스트가 있지만 edge case가 부족한 모듈
- E2E 테스트 시나리오 중 누락된 것
- 결과를 `.sync/agents/agent-5-test.md`에 정리

## 테스트 실행 방법
```bash
# 단위 테스트
pnpm test

# 특정 파일
pnpm test -- src/gateway/auth.test.ts

# E2E
pnpm test:e2e  # 또는 vitest.e2e.config.ts 사용
```

## 산출물
- 테스트 검증 리포트 (`.sync/agents/agent-5-test.md`에 작업 로그)
- 가이드 문서 코드 예제 검증 결과
- 테스트 갭 분석
- 가능하면 누락 테스트 코드 작성

## 주의사항
- 기존 테스트를 수정하지 말 것 (추가만)
- 테스트 실행 시 외부 API 호출하는 live 테스트는 건드리지 말 것
- 커밋하지 말 것 (팀장이 리뷰 후 커밋)
