# 채널 활용 가이드

OpenClaw는 Grammy.js 기반 멀티채널 AI 에이전트 프레임워크로, Telegram, Discord, WhatsApp, Slack, Signal 등 7개 core 채널과 31개 extension 플러그인을 지원한다.

---

## 목차

1. [Telegram 봇 생성 및 연결](#1-telegram-봇-생성-및-연결)
2. [Telegram 고급 기능](#2-telegram-고급-기능)
3. [Telegram 미디어 처리](#3-telegram-미디어-처리)
4. [Discord 활용](#4-discord-활용)
5. [WhatsApp (WebChat)](#5-whatsapp-webchat)
6. [WebChat (Gateway)](#6-webchat-gateway)
7. [카카오톡 연동 방안](#7-카카오톡-연동-방안)
8. [채널 기능 비교표](#8-채널-기능-비교표)
9. [멀티채널 라우팅](#9-멀티채널-라우팅)
10. [보안 설정](#10-보안-설정)

---

## 1. Telegram 봇 생성 및 연결

### 1.1 BotFather로 봇 생성

1. Telegram에서 [@BotFather](https://t.me/BotFather)를 검색하여 대화 시작
2. `/newbot` 명령 입력
3. 봇 이름 입력 (예: "My OpenClaw Bot")
4. 봇 username 입력 (반드시 `Bot`으로 끝나야 함, 예: `MyOpenClawBot`)
5. BotFather가 발급한 **API 토큰** 복사 (형식: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 1.2 봇 설정 (선택)

```
/setdescription - 봇 설명 설정
/setabouttext   - "About" 섹션 설정
/setuserpic     - 프로필 사진 설정
/setcommands    - 명령어 목록 설정 (OpenClaw가 자동 등록 가능)
```

**그룹 채팅용 추가 설정:**
```
/setprivacy → Disable  (그룹 내 모든 메시지 수신)
/setjoingroups → Enable (그룹 초대 허용)
```

### 1.3 OpenClaw에 토큰 연결

**방법 1: CLI 명령**
```bash
openclaw config set channels.telegram.botToken "YOUR_BOT_TOKEN"
```

**방법 2: 설정 파일 직접 수정**
```yaml
channels:
  telegram:
    botToken: "123456789:ABCdefGHIjklMNOpqrsTUVwxyz"
```

**방법 3: 시크릿 파일 참조**
```yaml
channels:
  telegram:
    tokenFile: "/path/to/telegram-token.secret"
```

> 참고: `src/config/types.telegram.ts`에서 `TelegramAccountConfig` 타입 확인 가능

### 1.4 Polling vs Webhook 모드

#### Polling 모드 (기본값)

별도 설정 없이 동작. OpenClaw가 Telegram 서버에 주기적으로 새 메시지를 요청한다.

- 장점: 방화벽/NAT 환경에서도 동작, 설정 간단
- 단점: 약간의 지연 발생, 서버 리소스 소비

내부 동작:
- `sequentialize()` 미들웨어로 메시지 순서 보장
- `createTelegramUpdateDedupe()`로 중복 업데이트 제거
- `@grammyjs/transformer-throttler`로 API 속도 제한 준수

> 관련 코드: `src/telegram/bot.ts`, `src/telegram/bot-updates.ts`

#### Webhook 모드

```yaml
channels:
  telegram:
    webhookUrl: "https://your-domain.com/telegram/webhook"
    webhookSecret: "your-random-secret"   # 선택
    webhookPath: "/telegram/webhook"       # 선택
```

- 장점: 실시간 메시지 수신, 리소스 효율적
- 단점: HTTPS 공인 도메인 필요, SSL 인증서 필요
- 헬스체크 엔드포인트: `/healthz`

> 관련 코드: `src/telegram/webhook.ts`

### 1.5 네트워크/프록시 설정

```yaml
channels:
  telegram:
    proxy: "socks5://user:pass@host:port"
    timeoutSeconds: 30
    network:
      autoSelectFamily: false  # Node 22+ DNS 동작 제어
```

환경 변수로도 제어 가능:
- `OPENCLAW_TELEGRAM_ENABLE_AUTO_SELECT_FAMILY`
- `OPENCLAW_TELEGRAM_DISABLE_AUTO_SELECT_FAMILY`

> 관련 코드: `src/telegram/network-config.ts`

### 1.6 멀티 계정

```yaml
channels:
  telegram:
    accounts:
      main:
        botToken: "TOKEN_1"
        dmPolicy: "pairing"
      secondary:
        botToken: "TOKEN_2"
        dmPolicy: "allowlist"
        allowFrom: ["user1", "user2"]
```

---

## 2. Telegram 고급 기능

### 2.1 그룹 채팅

#### 기본 정책

```yaml
channels:
  telegram:
    groupPolicy: "open"       # "open" | "disabled" | "allowlist"
    groupAllowFrom: ["admin1", "admin2"]  # allowlist 모드 시
```

#### 그룹별 세부 설정

```yaml
channels:
  telegram:
    groups:
      "-1001234567890":        # 그룹 chat ID
        enabled: true
        requireMention: true   # @봇이름 멘션 필수
        allowFrom: ["user1"]   # 이 그룹 전용 허용 목록
        systemPrompt: "이 그룹에서는 한국어로 대화합니다."
        skills: ["translate", "summarize"]  # 스킬 필터
```

#### Forum (토픽) 지원

Telegram 슈퍼그룹의 Forum/Topic 기능을 지원한다:

```yaml
channels:
  telegram:
    groups:
      "-1001234567890":
        topics:
          "12345":             # message_thread_id
            enabled: true
            requireMention: false
            systemPrompt: "이 토픽 전용 프롬프트"
            skills: ["coding"]
```

#### Mention Gating

`requireMention: true` 설정 시 봇 이름을 멘션해야만 응답한다. 그룹 내 불필요한 응답을 방지할 때 유용하다.

### 2.2 인라인 버튼

```yaml
channels:
  telegram:
    capabilities:
      inlineButtons: "all"     # "off" | "dm" | "group" | "all" | "allowlist"
```

기본값: `"allowlist"`

인라인 버튼은 커맨드 인자 메뉴로 사용되며, 콜백 데이터에 직렬화된 커맨드 인자가 포함된다.

> 관련 코드: `src/telegram/inline-buttons.ts`, `src/telegram/send.ts`의 `buildInlineKeyboard()`

### 2.3 네이티브 커맨드

```yaml
channels:
  telegram:
    commands:
      native: "auto"          # true | false | "auto"
      nativeSkills: true       # 스킬도 커맨드로 등록
    customCommands:
      - command: "help"
        description: "도움말 표시"
      - command: "status"
        description: "현재 상태 확인"
```

- `native: "auto"`: 자동으로 Telegram 커맨드 등록 (`bot.api.setMyCommands()`)
- 커맨드 이름 제약: a-z, 0-9, 밑줄만 허용, 최대 32자
- 플러그인 커맨드도 자동 등록 가능

> 관련 코드: `src/telegram/bot-native-commands.ts`

### 2.4 스티커 캐시

Telegram 스티커 팩을 캐시하여 성능을 최적화한다.

> 관련 코드: `src/telegram/sticker-cache.ts`

### 2.5 리액션

```yaml
channels:
  telegram:
    reactionNotifications: "own"   # "off" | "own" | "all"
    reactionLevel: "minimal"       # "off" | "ack" | "minimal" | "extensive"
    actions:
      reactions: true
```

### 2.6 스트리밍

```yaml
channels:
  telegram:
    streamMode: "block"            # "off" | "partial" | "block"
    blockStreaming: true
    blockStreamingCoalesce:
      minChars: 100
      maxChars: 4000
      idleMs: 500
```

### 2.7 메시지 청킹

```yaml
channels:
  telegram:
    textChunkLimit: 4000           # 기본값: 4000자
    chunkMode: "newline"           # "length" | "newline"
    replyToMode: "first"           # "off" | "first" | "all"
```

---

## 3. Telegram 미디어 처리

### 3.1 다운로드

지원 형식: 사진, 동영상, 문서, 오디오, 음성메시지, 스티커

```yaml
channels:
  telegram:
    mediaMaxMb: 5                  # 최대 다운로드 크기 (기본: 5MB)
```

> 관련 코드: `src/telegram/download.ts`

### 3.2 전송

미디어 전송 시 캡션 자동 분할 처리:

> 관련 코드: `src/telegram/send.ts`, `src/telegram/caption.ts`

### 3.3 음성

음성 메시지 호환성 처리:

> 관련 코드: `src/telegram/voice.ts`

---

## 4. Discord 활용

### 4.1 봇 생성 및 토큰 발급

1. [Discord Developer Portal](https://discord.com/developers/applications) 접속
2. "New Application" → 이름 입력 → "Create"
3. **Bot** 탭 → "Add Bot" → "Yes, do it!"
4. "Reset Token" → 토큰 복사
5. **Privileged Gateway Intents** 활성화:
   - MESSAGE CONTENT INTENT (필수)
   - SERVER MEMBERS INTENT (선택 - `intents.guildMembers: true` 시)
   - PRESENCE INTENT (선택 - `intents.presence: true` 시)

### 4.2 봇 초대

OAuth2 → URL Generator에서:
- Scopes: `bot`, `applications.commands`
- Bot Permissions: `Send Messages`, `Read Message History`, `Add Reactions`, `Attach Files`, `Embed Links`, `Use Slash Commands`

생성된 URL로 서버에 봇 초대.

### 4.3 OpenClaw 설정

```yaml
channels:
  discord:
    token: "YOUR_DISCORD_BOT_TOKEN"

    dm:
      enabled: true
      policy: "pairing"          # "pairing" | "allowlist" | "open" | "disabled"
      allowFrom: ["user_id_1"]
      groupEnabled: false        # 그룹 DM (기본: false)

    groupPolicy: "open"

    guilds:
      "123456789012345678":      # 서버(길드) ID
        slug: "my-server"
        requireMention: true
        channels:
          "987654321098765432":   # 채널 ID
            enabled: true
            requireMention: false
            systemPrompt: "이 채널 전용 프롬프트"
            skills: ["coding"]
            allowFrom: ["user1"]

    textChunkLimit: 2000         # Discord 메시지 한도
    maxLinesPerMessage: 17       # 소프트 라인 제한
    mediaMaxMb: 25

    intents:
      presence: false
      guildMembers: false

    actions:
      reactions: true
      stickers: true
      polls: true
      threads: true
      pins: true
      search: true
      memberInfo: true
      moderation: false
```

### 4.4 Discord 기능

**지원 기능:**
- 슬래시 커맨드 (네이티브 커맨드)
- 스레드 생성/관리
- 리액션, 스티커, 투표
- 채널/길드별 바인딩
- 실행 승인 포워딩 (`execApprovals`)

**실행 승인 포워딩:**
```yaml
channels:
  discord:
    execApprovals:
      enabled: true
      approvers: ["user_id_1", "user_id_2"]
      agentFilter: ["agent1"]
      sessionFilter: ["session1"]
```

**블록 스트리밍:**
```yaml
channels:
  discord:
    blockStreaming: true
    blockStreamingCoalesce:
      minChars: 1500
      idleMs: 1000
```

**Capabilities:**
- chatTypes: `direct`, `channel`, `thread`
- polls, reactions, threads, media, nativeCommands 지원

> 관련 코드: `extensions/discord/src/channel.ts`, `src/config/types.discord.ts`

---

## 5. WhatsApp (WebChat)

### 5.1 인증

WhatsApp Web 프로토콜(Baileys 라이브러리) 기반. QR 코드 스캔으로 로그인한다.

```yaml
channels:
  whatsapp:
    authDir: "./whatsapp-auth"   # 인증 상태 저장 디렉토리
    selfChatMode: false          # 개인 번호 사용 모드
```

### 5.2 설정

```yaml
channels:
  whatsapp:
    dmPolicy: "pairing"
    allowFrom: ["+821012345678"]   # E.164 형식 필수

    groupPolicy: "open"
    groupAllowFrom: ["+821098765432"]

    groups:
      "group_id@g.us":
        requireMention: true

    sendReadReceipts: true         # 읽음 표시 (기본: true)
    textChunkLimit: 4000
    mediaMaxMb: 50                 # 기본: 50MB

    ackReaction:
      emoji: "👀"
      direct: true
      group: "mentions"            # "always" | "mentions" | "never"

    debounceMs: 500                # 빠른 연속 메시지 배치 처리

    actions:
      reactions: true
      sendMessage: true
      polls: true
```

### 5.3 특이사항

- 허용 목록에 **E.164 형식** 전화번호 사용 (예: `+821012345678`)
- 네이티브 커맨드 미지원
- `selfChatMode`로 "나에게 보내기" 방식 사용 가능

> 관련 코드: `src/config/types.whatsapp.ts`

---

## 6. WebChat (Gateway)

### 6.1 아키텍처

WebChat은 별도 채널이 아닌 Gateway WebSocket 엔드포인트에 직접 연결하는 방식이다. macOS/iOS SwiftUI 네이티브 UI가 제공된다.

### 6.2 설정

```yaml
gateway:
  port: 3000
  bind: "loopback"               # "loopback" 또는 IP 주소
  auth:
    mode: "required"             # "required" | "off"
    token: "your-auth-token"
    password: "your-password"
  remote:                        # 원격 접속용
    url: "https://your-domain.com"
    token: "remote-token"
    password: "remote-password"
```

### 6.3 RPC 메서드

- `chat.send` - 메시지 전송
- `chat.history` - 대화 내역 조회
- `chat.inject` - 어시스턴트 노트 삽입

### 6.4 특징

- WebSocket 기반 실시간 통신 (polling 없음)
- SSH/Tailscale 터널을 통한 원격 모드 지원
- 별도 정적 서버 불필요

> 관련 문서: `docs/web/webchat.md`

---

## 7. 카카오톡 연동 방안

OpenClaw는 카카오톡을 공식 지원하지 않는다. 아래 3가지 우회 방안을 비교한다.

### 7.1 방안 비교표

| 항목 | 카카오 i 오픈빌더 + 웹훅 | 상담톡 API + 중간 레이어 | 커스텀 Extension 개발 |
|------|--------------------------|--------------------------|----------------------|
| **난이도** | 중 | 상 | 상 |
| **비용** | 무료 (기본) | 유료 (상담톡 계약) | 개발 비용만 |
| **양방향 대화** | 제한적 (폴백 블록) | 가능 (웹훅 수신) | 가능 |
| **메시지 수신** | 오픈빌더 스킬로 수신 | 상담톡 웹훅으로 수신 | 직접 구현 필요 |
| **메시지 발신** | 스킬 응답으로만 가능 | API로 자유 발신 | API 구현 필요 |
| **응답 형식** | 카카오 응답 포맷 제약 | 상담톡 포맷 제약 | 자유 |
| **실현 가능성** | 높음 | 중간 | 낮음 |
| **유지보수** | 카카오 의존적 | 해피톡 등 중간서비스 의존 | 자체 관리 |

### 7.2 방안 A: 카카오 i 오픈빌더 + 웹훅 브릿지

**구조:**
```
사용자 → 카카오톡 → 오픈빌더 → 스킬(Webhook) → OpenClaw API → 응답 → 오픈빌더 → 카카오톡
```

**설정 절차:**

1. [카카오 i 오픈빌더](https://i.kakao.com/) 접속 → 챗봇 생성
2. **스킬 생성**: 스킬 메뉴 → URL에 OpenClaw 웹훅 엔드포인트 입력
3. **폴백 블록 연결**: 시나리오 → 폴백 블록 → 파라미터 설정에서 스킬 선택 → 봇 응답을 "스킬 데이터"로 설정
4. **배포**: 배포 메뉴에서 배포 실행

**OpenClaw 측 구현:**

Gateway HTTP 핸들러로 카카오 스킬 요청을 처리하는 브릿지 서버가 필요하다:

```typescript
// 카카오 스킬 요청 형식
{
  "userRequest": {
    "utterance": "사용자 메시지",
    "user": { "id": "kakao_user_id" }
  }
}

// 카카오 스킬 응답 형식
{
  "version": "2.0",
  "template": {
    "outputs": [{
      "simpleText": { "text": "OpenClaw 응답" }
    }]
  }
}
```

**제약사항:**
- 응답 시간 5초 제한 (타임아웃 시 실패)
- LLM 응답이 5초를 초과하면 비동기 처리 필요
- 카카오 응답 포맷만 사용 가능 (SimpleText, SimpleImage, BasicCard 등)

### 7.3 방안 B: 상담톡 API + n8n 중간 레이어

**구조:**
```
사용자 → 카카오톡 → 상담톡 → 해피톡 웹훅 → n8n → OpenClaw API → n8n → 상담톡 API → 카카오톡
```

[해피톡 상담톡 웹훅](https://developer-center.happytalk.io/KakaoWebhook/)을 통해 메시지 수신/발신이 가능하다.

**n8n 워크플로우:**
1. Webhook 트리거: 해피톡에서 메시지 수신
2. HTTP Request: OpenClaw Gateway API로 메시지 전달
3. HTTP Request: 응답을 상담톡 API로 발신

**제약사항:**
- 상담톡 계약 및 비용 발생
- 해피톡 등 중간 서비스 의존
- [카카오톡 채널 API 공식 문서](https://developers.kakao.com/docs/latest/ko/kakaotalk-channel/callback)에 따르면 메시지 수신 API가 공식 제공되지 않음 → 상담톡 우회 필수

### 7.4 방안 C: 커스텀 Extension 플러그인 개발

OpenClaw의 extension 구조를 활용하여 카카오톡 플러그인을 직접 개발하는 방안.

**Extension 플러그인 구조:**

```
extensions/kakaotalk/
├── index.ts                    # 플러그인 엔트리포인트
├── openclaw.plugin.json        # 메타데이터
├── package.json
└── src/
    ├── channel.ts              # ChannelPlugin 구현
    ├── runtime.ts              # 런타임 싱글톤
    ├── config-schema.ts        # Zod 설정 스키마
    ├── accounts.ts             # 계정 처리
    └── actions.ts              # 메시지 액션
```

**엔트리포인트 (`index.ts`):**
```typescript
import type { OpenClawPluginApi } from "openclaw/plugin-sdk";

const plugin = {
  id: "kakaotalk",
  name: "KakaoTalk",
  description: "KakaoTalk channel plugin via 오픈빌더 bridge",
  configSchema: kakaotalkConfigSchema,
  register(api: OpenClawPluginApi) {
    api.registerChannel({ plugin: kakaotalkPlugin });
    api.registerHttpHandler(handleKakaoWebhookRequest);
  },
};

export default plugin;
```

**ChannelPlugin 구현 시 필수 인터페이스:**

```typescript
{
  id: "kakaotalk",
  meta: { name: "KakaoTalk", icon: "..." },
  capabilities: {
    chatTypes: ["direct"],
    media: false,         // 카카오 제약
    reactions: false,
    threads: false,
    nativeCommands: false,
  },
  security: { resolveDmPolicy: (ctx) => ... },
  config: { listAccountIds, resolveAccount, isConfigured, ... },
  outbound: {
    deliveryMode: "gateway",
    sendText: async (params) => { /* 카카오 스킬 응답 반환 */ },
  },
  gateway: {
    startAccount: async (ctx) => { /* HTTP 웹훅 리스너 시작 */ },
  },
}
```

> 참고: 기존 BlueBubbles 플러그인(`extensions/bluebubbles/`) 구조를 템플릿으로 활용 가능

**제약사항:**
- 카카오톡 메시지 수신 공식 API 부재로 오픈빌더 스킬 웹훅에 의존
- 응답 시간 5초 제한 문제 해결 필요
- 상당한 개발 공수 필요

### 7.5 권장 사항

**단기**: 방안 A (카카오 i 오픈빌더 + 웹훅 브릿지)가 가장 현실적. 무료이고 설정이 간단하지만, 5초 응답 제한을 우회하기 위해 "처리 중..." 즉시 응답 + 후속 알림톡 패턴이 필요할 수 있다.

**장기**: 방안 C (커스텀 Extension)로 발전시키되, 카카오 API의 근본적 제약(메시지 수신 API 부재)이 해소되어야 완전한 양방향 통합이 가능하다.

---

## 8. 채널 기능 비교표

`src/channels/plugins/types.core.ts`의 `ChannelCapabilities` 인터페이스 기반:

| 기능 | Telegram | Discord | WhatsApp | Slack | Signal | iMessage | WebChat |
|------|:--------:|:-------:|:--------:|:-----:|:------:|:--------:|:-------:|
| DM | O | O | O | O | O | O | O |
| 그룹 | O | - | O | O | O | O | - |
| 채널/서버 | O | O | - | O | - | - | - |
| 스레드 | O (토픽) | O | - | O | - | - | - |
| 미디어 전송 | O | O | O | O | O | O | - |
| 리액션 | O | O | O | O | O | O | - |
| 투표 | - | O | O | - | - | - | - |
| 네이티브 커맨드 | O | O (슬래시) | - | O | - | - | - |
| 블록 스트리밍 | O | O | O | O | - | - | O |
| 메시지 편집 | O | O | - | O | - | - | - |
| 메시지 삭제 | O | O | - | O | - | - | - |
| 인라인 버튼 | O | - | - | O | - | - | - |
| DM 페어링 | O | O | O | O | O | - | - |
| 미디어 최대 크기 | 5MB | 25MB | 50MB | - | - | - | - |
| 텍스트 청크 | 4000자 | 2000자 | 4000자 | - | - | - | - |

> `O` = 지원, `-` = 미지원 또는 해당 없음

---

## 9. 멀티채널 라우팅

### 9.1 에이전트 바인딩

`bindings` 설정으로 특정 채널/계정/피어에 에이전트를 매핑한다:

```yaml
bindings:
  - agentId: "default"
    match:
      channel: "telegram"
      accountId: "main"

  - agentId: "support-bot"
    match:
      channel: "discord"
      peer:
        kind: "channel"
        id: "987654321098765432"

  - agentId: "korean-agent"
    match:
      channel: "telegram"
      peer:
        kind: "group"
        id: "-1001234567890"
```

### 9.2 라우트 해석 우선순위

`resolveAgentRoute()` 함수는 다음 순서로 매칭한다:

1. `binding.peer` — 정확한 피어 매칭
2. `binding.guild` — Discord 길드
3. `binding.team` — Slack/Teams
4. `binding.account` — 특정 계정
5. `binding.channel` — 채널 와일드카드 `"*"`
6. default — 폴백 에이전트

> 관련 코드: `src/routing/resolve-route.ts`, `src/routing/bindings.ts`

### 9.3 세션 키 구조

세션 키 형식:
```
agent:{agentId}:{channel}:{chatType}:{peerId}
```

예시:
- `agent:default:telegram:dm:12345`
- `agent:default:telegram:group:-67890`
- `agent:custom:discord:channel:987654321`

### 9.4 DM 스코프

```yaml
session:
  dmScope: "per-peer"         # "main" | "per-peer" | "per-channel-peer" | "per-account-channel-peer"
```

| 모드 | 설명 |
|------|------|
| `main` | 모든 DM이 하나의 세션 공유 |
| `per-peer` | 상대방별 별도 세션 |
| `per-channel-peer` | 채널+상대방별 별도 세션 |
| `per-account-channel-peer` | 계정+채널+상대방별 별도 세션 |

### 9.5 크로스 플랫폼 ID 링크

```yaml
session:
  identityLinks:
    "user-alice":
      - "telegram:12345"
      - "discord:67890"
      - "whatsapp:+821012345678"
```

### 9.6 세션 리셋

```yaml
session:
  reset:
    mode: "idle"               # "daily" | "idle"
    idleMinutes: 60
  resetByType:
    dm:
      mode: "idle"
      idleMinutes: 120
    group:
      mode: "daily"
      atHour: 0
```

> 관련 코드: `src/routing/session-key.ts`

---

## 10. 보안 설정

### 10.1 DM 페어링 시스템

DM 페어링은 알 수 없는 사용자가 봇에 처음 메시지를 보낼 때 인증하는 메커니즘이다.

**동작 방식:**

1. 사용자가 봇에 첫 메시지 전송
2. 봇이 8자 페어링 코드 생성 (예: `ABC12345`)
3. 사용자에게 "페어링 코드를 소유자에게 전달하세요" 안내
4. 소유자가 CLI/UI에서 코드 승인
5. 해당 사용자가 허용 목록에 추가됨

```yaml
channels:
  telegram:
    dmPolicy: "pairing"       # 페어링 모드 활성화
```

**내부 처리:**
```typescript
// 페어링 요청 생성
upsertTelegramPairingRequest({ chatId, username, firstName, lastName })
// → { code: "ABC12345", created: true }

// 소유자 승인
approveTelegramPairingCode({ code: "ABC12345" })
// → 허용 목록에 추가 + 사용자에게 승인 메시지 전송
```

> 관련 코드: `src/telegram/pairing-store.ts`

### 10.2 DM 정책 비교

| 정책 | 설명 | 사용 시나리오 |
|------|------|-------------|
| `pairing` | 페어링 코드로 인증 (기본값) | 개인용, 소규모 팀 |
| `allowlist` | 사전 등록 사용자만 허용 | 고정 멤버 환경 |
| `open` | 모든 DM 허용 (`allowFrom: ["*"]` 필요) | 공개 봇 |
| `disabled` | 모든 DM 차단 | 그룹 전용 봇 |

### 10.3 허용 목록 설정

**정적 설정:**
```yaml
channels:
  telegram:
    allowFrom: ["user123", "@username", "12345"]

  discord:
    dm:
      allowFrom: ["user_id_1", "user_id_2"]

  whatsapp:
    allowFrom: ["+821012345678"]   # E.164 형식
```

**동적 저장소:** 페어링 승인 시 자동으로 허용 목록에 추가되며, 설정 파일의 정적 목록과 병합된다.

### 10.4 그룹 권한 정책

```yaml
channels:
  telegram:
    groupPolicy: "allowlist"
    groupAllowFrom: ["admin1", "admin2"]

    groups:
      "-1001234567890":
        allowFrom: ["mod1", "mod2"]   # 그룹별 추가 허용
```

| 정책 | 설명 |
|------|------|
| `open` | 모든 그룹 멤버 허용 (멘션 게이팅 가능) |
| `allowlist` | `groupAllowFrom` + 그룹별 `allowFrom`만 허용 |
| `disabled` | 모든 그룹 메시지 차단 |

### 10.5 Per-DM 오버라이드

특정 사용자에 대해 세부 권한 설정:

```yaml
channels:
  telegram:
    dms:
      "12345":
        enabled: true
        skills: ["translate"]
        systemPrompt: "이 사용자 전용 프롬프트"
        tools:
          policy: "denylist"
          deny: ["dangerous_tool"]
```

### 10.6 그룹 도구 정책

```yaml
channels:
  telegram:
    groups:
      "-1001234567890":
        tools:
          policy: "allowlist"
          allow: ["safe_tool_1", "safe_tool_2"]
        toolsBySender:
          "admin_user":
            policy: "allow"            # 관리자는 모든 도구 허용
```

---

## 참고 자료

### 프로젝트 내부 코드 참조

| 파일 | 설명 |
|------|------|
| `src/config/types.telegram.ts` | Telegram 설정 타입 정의 |
| `src/config/types.discord.ts` | Discord 설정 타입 정의 |
| `src/config/types.whatsapp.ts` | WhatsApp 설정 타입 정의 |
| `src/config/types.openclaw.ts` | 최상위 설정 구조 |
| `src/channels/plugins/types.core.ts` | ChannelCapabilities 타입 |
| `src/telegram/bot.ts` | Telegram 봇 생성 |
| `src/telegram/webhook.ts` | Webhook 모드 설정 |
| `src/telegram/network-config.ts` | 네트워크/프록시 설정 |
| `src/telegram/bot-native-commands.ts` | 네이티브 커맨드 등록 |
| `src/telegram/inline-buttons.ts` | 인라인 버튼 |
| `src/telegram/pairing-store.ts` | DM 페어링 저장소 |
| `src/routing/bindings.ts` | 에이전트 바인딩 |
| `src/routing/resolve-route.ts` | 라우트 해석 |
| `src/routing/session-key.ts` | 세션 키 생성 |
| `extensions/discord/src/channel.ts` | Discord 플러그인 |
| `extensions/bluebubbles/` | Extension 구조 참고용 |

### 외부 참조

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Discord Developer Portal](https://discord.com/developers/docs)
- [카카오 i 오픈빌더](https://i.kakao.com/)
- [카카오톡 채널 웹훅 API](https://developers.kakao.com/docs/latest/ko/kakaotalk-channel/callback)
- [해피톡 상담톡 웹훅](https://developer-center.happytalk.io/KakaoWebhook/)
- [n8n 카카오톡 연동 사례 (GPTers)](https://www.gpters.org/nocode/post/create-chatbot-n8n-create-efWD8L8dCetwryc)
- [n8n 카카오톡 Bridge Bot (GPTers)](https://www.gpters.org/chatbot/post/you-can-send-anything-hyxeue7F8w1YpdW)
