# 섹션 1: OpenClaw(오픈크롤봇) 완전정복

## 1.1 OpenClaw이란?

OpenClaw(구 Clawdbot → Moltbot → OpenClaw)은 **로컬 퍼스트 AI 에이전트 플랫폼**이다. 단순 챗봇이 아니라, 사용자의 컴퓨터에서 직접 실행되며 파일 조작, 쉘 명령 실행, 웹 브라우징 등을 수행하는 **행동하는 AI 비서**다.

### 핵심 차별점
| 특성 | OpenClaw | 일반 AI 챗봇 |
|------|----------|-------------|
| 실행 환경 | 로컬 머신 (self-hosted) | 클라우드 |
| 파일 접근 | 직접 읽기/쓰기 | 불가 |
| 쉘 명령 | 직접 실행 | 불가 |
| 멀티채널 | WhatsApp/Telegram/Discord/iMessage/Slack/Signal 등 | 웹 UI만 |
| 멀티에이전트 | 여러 에이전트 동시 운영 | 단일 세션 |
| 프라이버시 | 데이터가 로컬에 머묾 | 클라우드 전송 |
| 커스터마이징 | 스킬, SOUL.md, 워크스페이스 | 제한적 |

### 아키텍처 구조
```
WhatsApp / Telegram / Discord / iMessage (+ 플러그인)
     │
     ▼
┌───────────────────────────┐
│        Gateway            │  ws://127.0.0.1:18789
│    (단일 제어 평면)        │
│                           │  http://<host>:18793 (Canvas)
└───────────┬───────────────┘
            │
            ├─ Pi 에이전트 (RPC) ← 핵심 AI 두뇌
            ├─ CLI (openclaw …)
            ├─ Chat UI (SwiftUI)
            ├─ macOS 앱 (OpenClaw.app)
            ├─ iOS 노드 (페어링)
            └─ Android 노드 (페어링)
```

**Gateway**: 모든 채널 연결과 세션을 관리하는 단일 제어 평면. 하나의 장기 실행 프로세스.
**Pi Agent**: 실제 AI 추론과 도구 실행을 담당하는 코딩 에이전트 (RPC 모드).
**Skills**: 에이전트의 능력을 확장하는 플러그인 시스템 (SKILL.md + 스크립트).
**Nodes**: iOS/Android 디바이스를 페어링하여 카메라, 화면, 위치 등 활용.

## 1.2 핵심 기능 매트릭스

| 카테고리 | 기능 | 설명 |
|---------|------|------|
| **채널** | WhatsApp | Baileys (WhatsApp Web 프로토콜) |
| | Telegram | Bot API / grammY (DM + 그룹) |
| | Discord | Bot API / discord.js (DM + 길드) |
| | iMessage | imsg CLI (macOS) |
| | Slack | Bot token + WebSocket |
| | Signal | signal-cli 연동 |
| | Google Chat | Workspace 연동 |
| | MS Teams | Bot Framework |
| | Matrix | 플러그인 |
| | WebChat | 로컬 웹 UI |
| **에이전트** | 멀티에이전트 | 여러 에이전트를 하나의 Gateway에서 격리 운영 |
| | 멀티계정 | 채널당 여러 계정 (예: WhatsApp 개인+비즈) |
| | 세션 관리 | DM은 메인 세션으로 통합, 그룹은 격리 |
| | A2A 통신 | 에이전트 간 sessions_send/spawn |
| **도구** | 파일 읽기/쓰기 | 워크스페이스 내 파일 조작 |
| | 쉘 실행 | exec 도구로 명령 실행 |
| | 웹 브라우저 | 브라우저 자동화 (CDP) |
| | 웹 검색 | Brave Search API |
| | 크론 | 예약 작업 / 리마인더 |
| | 캔버스 | 웹뷰 기반 UI 표시 |
| | TTS | 텍스트-음성 변환 |
| **미디어** | 이미지 | 송수신 + 비전 분석 |
| | 음성 | 음성 메모 수신 + 전사 |
| | 문서 | PDF, 문서 첨부 처리 |
| **앱** | macOS | 네이티브 앱 + 메뉴바 |
| | iOS | 노드 페어링 + Canvas |
| | Android | 노드 + Chat + Camera |
| | Windows | WSL2 지원 |
| | Linux | 네이티브 지원 |
| **보안** | 샌드박스 | Docker 컨테이너 격리 |
| | OAuth | Anthropic/OpenAI 구독 인증 |
| | 화이트리스트 | 채널별 허용 사용자 제한 |
| | 도구 제한 | 에이전트별 도구 허용/거부 |

## 1.3 설치 가이드

### 사전 요구사항
- **Node.js ≥ 22** (권장: v24+)
- API 키: Anthropic (권장), OpenAI, 또는 Ollama (로컬)
- 채널: Telegram 봇 토큰 (@BotFather) 또는 WhatsApp

### 방법 1: npm 글로벌 설치 (권장)
```bash
# 설치
npm install -g openclaw@latest

# 온보딩 + 서비스 설치 (launchd/systemd)
openclaw onboard --install-daemon

# WhatsApp 페어링 (QR 표시)
openclaw channels login

# 게이트웨이 실행 (서비스 자동 실행)
openclaw gateway --port 18789
```

### 방법 2: 소스에서 빌드
```bash
git clone https://github.com/openclaw/openclaw.git
cd openclaw
pnpm install
pnpm ui:build
pnpm build
openclaw onboard --install-daemon
```

### 방법 3: Docker Compose
```bash
git clone https://github.com/openclaw/openclaw.git
cd openclaw
cp .env.example .env
# .env 편집: API 키, 텔레그램 토큰 등 설정
docker-compose up -d
```

### 방법 4: DigitalOcean 원클릭 / Vultr 배포
- DigitalOcean: [원클릭 배포 가이드](https://www.digitalocean.com/community/tutorials/how-to-run-openclaw)
- Vultr: [배포 문서](https://docs.vultr.com/how-to-deploy-openclaw-autonomous-ai-agent-platform)

### 방법 5: Cloudflare Workers
```bash
# cloudflare/moltworker 리포지토리 사용
npx wrangler secret put TELEGRAM_BOT_TOKEN
npm run deploy
```

### 텔레그램 설정
1. @BotFather에서 `/newbot` → 봇 토큰 수령
2. @userinfobot에서 자신의 User ID 확인
3. 환경변수 설정:
   ```
   TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
   ```
4. `openclaw.json`에서 허용 사용자 설정:
   ```json
   {
     "channels": {
       "telegram": {
         "allowFrom": ["12345678"]
       }
     }
   }
   ```

## 1.4 핵심 설정 (openclaw.json)

```json5
{
  // 에이전트 기본 설정
  agents: {
    defaults: {
      workspace: "~/.openclaw/workspace",
    },
    list: [
      {
        id: "main",
        identity: {
          name: "세나",
          emoji: "🦞",
        },
        groupChat: {
          mentionPatterns: ["@sena", "@세나"],
        },
      },
    ],
  },

  // 채널 설정
  channels: {
    telegram: {
      allowFrom: ["12345678"],  // Telegram User ID
      groups: {
        "*": { requireMention: true },
      },
    },
    whatsapp: {
      dmPolicy: "allowlist",
      allowFrom: ["+821012345678"],
    },
  },

  // 모델 설정
  models: {
    providers: {
      anthropic: {
        apiKey: "${ANTHROPIC_API_KEY}",
      },
    },
  },

  // 보안
  tools: {
    agentToAgent: {
      enabled: true,
      allow: ["sena", "miru", "hana", "yuri"],
    },
  },
}
```

## 1.5 고급 설정

### 멀티에이전트 라우팅
```json5
{
  agents: {
    list: [
      { id: "chat", model: "anthropic/claude-sonnet-4-5" },
      { id: "opus", model: "anthropic/claude-opus-4-5" },
    ],
  },
  bindings: [
    { agentId: "chat", match: { channel: "whatsapp" } },
    { agentId: "opus", match: { channel: "telegram" } },
  ],
}
```

### 환경변수 치환
```json5
{
  models: {
    providers: {
      "custom": {
        apiKey: "${CUSTOM_API_KEY}",
        baseUrl: "${CUSTOM_API_BASE}/v1",
      },
    },
  },
}
```

### Config Include (분할 설정)
```json5
// openclaw.json
{
  agents: { $include: "./agents.json5" },
  channels: { $include: "./channels.json5" },
}
```

### 에이전트별 샌드박스
```json5
{
  agents: {
    list: [
      {
        id: "trusted",
        sandbox: { mode: "off" },
      },
      {
        id: "untrusted",
        sandbox: { mode: "all", scope: "agent" },
        tools: {
          allow: ["read"],
          deny: ["exec", "write"],
        },
      },
    ],
  },
}
```

## 1.6 알려진 버그 & 해결법

| 문제 | 원인 | 해결 |
|------|------|------|
| 모델 404 에러로 봇 행 | 만료된 모델(예: gemini-1.5-pro) 사용 시 failover 미작동 | 모델명 업데이트 또는 failover 설정 (GitHub #4992) |
| WhatsApp 세션 끊김 | Baileys 세션 만료 | `openclaw channels login` 재실행 |
| 텔레그램 Webhook 실패 | NAT 뒤에서 Webhook 사용 | Polling 모드 사용 (기본값) |
| 스킬 로드 안 됨 | 바이너리 미설치 | `openclaw doctor` 실행, 누락 바이너리 설치 |
| 설정 검증 실패로 부팅 불가 | 잘못된 설정 키 | `openclaw doctor --fix` 실행 |
| 권한 문제 (Docker) | 컨테이너 내 UID 불일치 | 볼륨 소유권 확인 |
| 에이전트 간 통신 실패 | A2A 미활성화 | `tools.agentToAgent.enabled: true` |
| glm-4.7 도구 호출 실패 | 모델이 tool call을 thinking 블록에 삽입 | 다른 모델로 전환 |

## 1.7 버전 히스토리 (주요)

| 버전 | 날짜 | 주요 변경 |
|------|------|----------|
| v2026.1.29 | 2026-01-30 | Telegram AccountId 멀티에이전트, 비디오 노트, TTS 런타임 개선 |
| v2026.1.23 | 2026-01-23 | Telegram 마크다운 캡션, MS Teams 개선 |
| v2026.1.6 | 2026-01 | 에이전트별 샌드박스/도구 제한 |
| (이전) | - | Clawdbot → Moltbot → OpenClaw 리브랜딩 |

## 1.8 세팅 체크리스트

- [ ] Node.js ≥ 22 설치 확인
- [ ] `npm install -g openclaw@latest`
- [ ] `openclaw onboard --install-daemon`
- [ ] API 키 설정 (Anthropic/OpenAI)
- [ ] 채널 봇 토큰 설정 (Telegram/WhatsApp)
- [ ] `openclaw.json` 생성 (allowFrom 필수!)
- [ ] `openclaw doctor` 실행 → 문제 확인
- [ ] 테스트 메시지 전송
- [ ] 보안 설정 (allowFrom, 도구 제한)
- [ ] `openclaw security audit --deep` 실행 → 보안 취약점 확인
- [ ] 스킬 설치 (필요한 것만)
- [ ] 백업 설정 (cron + 스크립트)

## 1.9 트러블슈팅 FAQ

**Q: 봇이 응답하지 않아요**
A: `openclaw status` → `openclaw logs --follow`로 로그 확인. allowFrom에 자신의 ID가 있는지 체크.

**Q: "version no longer supported" 에러**
A: 모델 프로바이더가 해당 모델을 폐기한 것. `openclaw.json`에서 모델명 업데이트.

**Q: 그룹에서 봇이 모든 메시지에 반응해요**
A: `groups: { "*": { requireMention: true } }` 설정하고, `mentionPatterns` 지정.

**Q: 스킬이 보이지 않아요**
A: `openclaw doctor`로 스킬 상태 확인. `requires.bins`에 명시된 바이너리 설치 필요.

**Q: 멀티에이전트 간 통신이 안 돼요**
A: `tools.agentToAgent.enabled: true` + `allow` 배열에 에이전트 ID 추가.

---

## 1.10 한국 커뮤니티 반응 & 실전 후기

### 주요 반응
- **"자비스가 현실로?"** — 맥미니 품절 사태와 함께 한국에서도 폭발적 관심 (twofootdog)
- **"Siri가 약속했지만 이루지 못한 것"** — 실제 행동하는 AI 비서로 평가 (갓대희)
- **비용 우려** → 무료 사용법 가이드 등장: Gemini CLI, Ollama 연동으로 $0 운영 가능 (twofootdog)
- **보안 경고** — 수백 개 서버가 인증 없이 노출, API 키·대화 기록·원격 코드 실행 위험 (Tech42, Forbes)
- **리브랜딩 혼란** — Clawdbot → Moltbot → OpenClaw 3번 이름 변경에 커뮤니티 피로감 (잇싸, ZDNet)

### Hacker News 실사용 팁 (2026-01-31)
1. **Docker/VPS 격리 필수**: "로컬 머신에 직접 설치하는 건 좀 미쳤다" — HN 사용자
2. **Gmail 보안**: 별도 @gmail.com 계정 생성 → 메인 이메일 포워딩 → OpenClaw에는 부계정만 연결
3. **프롬프트 인젝션 경고**: 이메일/캘린더 연동 시 악의적 메일이 에이전트를 조작할 수 있음
4. **로컬 모델 추천**: M1 MacBook/Mac Mini + Ollama로 무료 운영. "로컬 모델이 이전보다 미친 듯이 좋아졌다"
5. **Heartbeat 기능**: 크론이 30분마다 실행되어 세션 유지. 장시간 자동 작업 가능.
6. **Pi 엔진**: "Gemini 3 Flash/Pro가 도구를 루프 없이 일관되게 사용하는 유일한 에이전트"

### brunch 실사용 후기 핵심
- 맥미니 충동구매 → OpenClaw 설치 → "진짜 내 AI 비서"
- **모델 전환**: Claude rate limit → GPT 전환 → GPT도 막히면 Gemini로
- 하나의 봇에서 3개 모델 지원, 대화 맥락 유지하며 모델만 교체

### SIR 개발자 커뮤니티
- "Claude Pro/Max로는 사용 안 되나?" → API 키(sk-ant-) 필수, 구독과 별개
- 하루 종일 돌리면 비용 우려 → OAuth 구독 인증으로 해결 가능

## 출처
- [OpenClaw 공식 문서](https://docs.openclaw.ai/)
- [GitHub: openclaw/openclaw](https://github.com/openclaw/openclaw)
- [DEV.to: Unleashing OpenClaw 2026](https://dev.to/mechcloud_academy/unleashing-openclaw-the-ultimate-guide-to-local-ai-agents-for-developers-in-2026-3k0h)
- [DataCamp: Moltbot Tutorial](https://www.datacamp.com/tutorial/moltbot-clawdbot-tutorial)
- [DigitalOcean: How to Run OpenClaw](https://www.digitalocean.com/community/tutorials/how-to-run-openclaw)
- [Vultr: Deploy OpenClaw](https://docs.vultr.com/how-to-deploy-openclaw-autonomous-ai-agent-platform)
- [Wikipedia: OpenClaw](https://en.wikipedia.org/wiki/OpenClaw)
- [한국 설치가이드 (twofootdog)](https://twofootdog.tistory.com/555)
- [갓대희 리뷰](https://goddaehee.tistory.com/504)
- [요즘IT 총정리](https://yozm.wishket.com/magazine/detail/3578/)
- [brunch: 실사용 후기](https://brunch.co.kr/@sungdairi/27)
- [brunch: 실리콘밸리가 미쳐버린 AI 도구](https://brunch.co.kr/@sungdairi/19)
- [Tech42: 보안 경고](https://www.tech42.co.kr/)
- [ZDNet: 강제 개명 논란](https://zdnet.co.kr/view/?no=20260128093735)
- [Forbes: Security Concerns](https://www.forbes.com/sites/ronschmelzer/2026/01/30/)
- [CNET: The Wild Ride](https://www.cnet.com/tech/services-and-software/from-clawdbot-to-moltbot-to-openclaw/)
- [Hacker News 토론](https://news.ycombinator.com/item?id=46820783)
- [SIR 개발자 커뮤니티](https://sir.kr/boards/ai/1469)
- [litmers: 완벽 가이드](https://litmers.com/blog/몰트봇-클로드봇-완벽가이드)
