# 섹션 5: 텔레그램 AI 비서 구축 가이드

## 5.1 구축 옵션 비교

### 옵션 비교표 (5x6 매트릭스)

| 항목 | OpenClaw 단독 | n8n + OpenClaw | Make + 봇 | Todoist API 직접 | 커스텀 봇 (Python) |
|------|-------------|---------------|-----------|-----------------|-------------------|
| **난이도** | ⭐⭐ 중 | ⭐⭐⭐ 중상 | ⭐⭐ 중 | ⭐⭐⭐⭐ 상 | ⭐⭐⭐⭐⭐ 최상 |
| **초기 비용** | $0 (설치) + API | $0 (self-host) / $20/월 (cloud) | $9/월~ | $0 | $0 |
| **월 운영 비용** | API $5-50 | API + 호스팅 | $9-29 + API | API만 | 호스팅 + API |
| **기능 범위** | ⭐⭐⭐⭐⭐ 광범위 | ⭐⭐⭐⭐⭐ 무제한 | ⭐⭐⭐ 중간 | ⭐⭐ 제한적 | ⭐⭐⭐⭐ 높음 |
| **확장성** | ⭐⭐⭐⭐ 스킬 시스템 | ⭐⭐⭐⭐⭐ 노드 무제한 | ⭐⭐⭐ 앱 의존 | ⭐⭐ API 한계 | ⭐⭐⭐⭐⭐ 무제한 |
| **유지보수** | ⭐⭐ 낮음 | ⭐⭐⭐ 중간 | ⭐ 최소 | ⭐⭐⭐ 중간 | ⭐⭐⭐⭐ 높음 |

## 5.2 추천 아키텍처

### 초급: OpenClaw 단독
```
Telegram → OpenClaw Gateway → AI 에이전트
                                  │
                              스킬 시스템
                              ├─ apple-reminders
                              ├─ gog (Gmail/Calendar)
                              ├─ weather
                              └─ summarize
```

**장점**: 설치 한 번이면 끝. 코딩 불필요.
**적합**: 개인 비서, 일정 관리, 리마인더, 이메일 요약

**설정 예시**:
```json5
{
  agents: {
    defaults: { workspace: "~/.openclaw/workspace" },
    list: [{
      id: "assistant",
      identity: { name: "비서", emoji: "📋" },
    }],
  },
  channels: {
    telegram: { allowFrom: ["YOUR_ID"] },
  },
}
```

**필수 스킬**:
```bash
# 스킬 설치
clawhub install apple-reminders
clawhub install gog        # Gmail + Calendar
clawhub install weather
clawhub install summarize
```

**크론 예시 (아침 브리핑)**:
```json5
{
  schedule: { kind: "cron", expr: "0 8 * * *", tz: "Asia/Seoul" },
  payload: {
    kind: "systemEvent",
    text: "아침 브리핑: 오늘 날씨, 일정, 미완료 작업 확인해서 알려줘"
  },
  sessionTarget: "main"
}
```

### 중급: OpenClaw + n8n 하이브리드
```
Telegram → OpenClaw Gateway → AI 에이전트
                                  │
                              웹훅 ↔ n8n
                                     │
                              ├─ Todoist API
                              ├─ Google Calendar
                              ├─ Notion API
                              ├─ Slack 알림
                              └─ 이메일 자동화
```

**장점**: OpenClaw의 AI + n8n의 자동화 파워. 코드 최소화.
**적합**: 복잡한 워크플로우, 외부 서비스 연동

**n8n 워크플로우 예시**:
1. Telegram 메시지 수신 (n8n Telegram Trigger)
2. AI 분석 (OpenAI/Claude 노드)
3. 의도 파악 → 분기
   - "할 일 추가" → Todoist API
   - "일정 확인" → Google Calendar
   - "메일 보내" → Gmail
4. 결과 → Telegram 답장

**참고 템플릿**:
- [n8n: Telegram → Todoist (음성/텍스트)](https://n8n.io/workflows/3052)
- [n8n: Multi-Modal AI Assistant](https://n8n.io/workflows/8793)

### 고급: 멀티에이전트 전문 비서
```
Telegram 그룹 → OpenClaw Gateway
                    │
        ┌───────────┼───────────┐
        ▼           ▼           ▼
    일정 에이전트  작업 에이전트  정보 에이전트
    (Calendar)   (Todoist)    (검색/요약)
        │           │           │
    gog 스킬    웹훅→n8n    web_search
                →Todoist    summarize
```

**장점**: 각 도메인 전문화, 병렬 처리, 확장 용이
**적합**: 파워유저, 팀 운영, 업무 자동화

## 5.3 Todoist 연동 구체적 방법

### 방법 A: OpenClaw 스킬로 직접 연동
```markdown
# skills/todoist/SKILL.md
---
name: todoist
description: Todoist 할 일 관리 (추가/조회/완료/삭제)
metadata: {"openclaw": {"requires": {"env": ["TODOIST_API_KEY"]}}}
---

## 사용법
Todoist API를 사용하여 할 일을 관리합니다.

### 할 일 추가
node {baseDir}/scripts/todoist.mjs add --content "회의 준비" --due "내일" --priority 2

### 할 일 조회
node {baseDir}/scripts/todoist.mjs list --filter "today"

### 할 일 완료
node {baseDir}/scripts/todoist.mjs complete --id TASK_ID

### 프로젝트 목록
node {baseDir}/scripts/todoist.mjs projects
```

**스크립트 (todoist.mjs)**:
```javascript
const API_KEY = process.env.TODOIST_API_KEY;
const BASE = "https://api.todoist.com/rest/v2";

const headers = {
  "Authorization": `Bearer ${API_KEY}`,
  "Content-Type": "application/json"
};

const args = process.argv.slice(2);
const cmd = args[0];

function getArg(name) {
  const idx = args.indexOf(`--${name}`);
  return idx >= 0 ? args[idx + 1] : null;
}

switch (cmd) {
  case "add": {
    const content = getArg("content");
    const due = getArg("due");
    const priority = parseInt(getArg("priority") || "1");
    const body = { content };
    if (due) body.due_string = due;
    if (priority) body.priority = priority;
    const res = await fetch(`${BASE}/tasks`, {
      method: "POST", headers,
      body: JSON.stringify(body)
    });
    const task = await res.json();
    console.log(`✅ 추가됨: "${task.content}" (ID: ${task.id}, 기한: ${task.due?.string || "없음"})`);
    break;
  }
  case "list": {
    const filter = getArg("filter") || "today";
    const res = await fetch(`${BASE}/tasks?filter=${encodeURIComponent(filter)}`, { headers });
    const tasks = await res.json();
    if (tasks.length === 0) {
      console.log("📭 할 일 없음");
    } else {
      tasks.forEach((t, i) => {
        console.log(`${i+1}. [${t.priority > 1 ? '❗' : '  '}] ${t.content} (기한: ${t.due?.string || "없음"}) [ID: ${t.id}]`);
      });
      console.log(`\n총 ${tasks.length}개`);
    }
    break;
  }
  case "complete": {
    const id = getArg("id");
    await fetch(`${BASE}/tasks/${id}/close`, { method: "POST", headers });
    console.log(`✅ 완료 처리됨 (ID: ${id})`);
    break;
  }
  case "delete": {
    const id = getArg("id");
    await fetch(`${BASE}/tasks/${id}`, { method: "DELETE", headers });
    console.log(`🗑️ 삭제됨 (ID: ${id})`);
    break;
  }
  case "projects": {
    const res = await fetch(`${BASE}/projects`, { headers });
    const projects = await res.json();
    projects.forEach(p => console.log(`- ${p.name} (ID: ${p.id})`));
    break;
  }
  default:
    console.log("사용법: node todoist.mjs <add|list|complete|delete|projects> [옵션]");
    console.log("  add --content '내용' --due '내일' --priority 2");
    console.log("  list --filter 'today'");
    console.log("  complete --id TASK_ID");
    console.log("  delete --id TASK_ID");
    console.log("  projects");
}
```

### 방법 B: n8n 워크플로우
1. OpenClaw에서 웹훅으로 의도 전달
2. n8n에서 Todoist 노드로 작업 생성
3. 결과를 Telegram으로 회신

### 방법 C: Apple Reminders 대체 (macOS)
```bash
clawhub install apple-reminders
```
macOS 사용자는 Todoist 대신 네이티브 Reminders 앱 활용 가능.

## 5.4 실제 구현 사례

### 사례 1: 음성 → 할 일 자동 생성
**워크플로우**: 텔레그램 음성 메모 → Whisper 전사 → AI 분석 → Todoist 생성
**결과**: 걸으면서 음성으로 할 일 추가. 자동으로 우선순위/기한 설정.

### 사례 2: 아침 브리핑 봇
**워크플로우**: 크론(08:00) → 날씨 + 캘린더 + 미완료 작업 → 텔레그램 전송
**결과**: 매일 아침 자동으로 오늘의 브리핑 수신.

### 사례 3: 이메일 → 작업 변환
**워크플로우**: Gmail 수신 → AI 분석 → 액션 아이템 추출 → Todoist/Reminders 생성
**결과**: 이메일 읽지 않아도 할 일이 자동 생성.

## 5.5 단계별 구현 가이드 (초급 → 실전)

### Step 1: OpenClaw 기본 설치 (30분)
```bash
npm install -g openclaw@latest
openclaw onboard --install-daemon
# Telegram 봇 토큰 설정
```

### Step 2: 필수 스킬 설치 (10분)
```bash
clawhub install apple-reminders  # 또는 todoist 스킬 직접 제작
clawhub install weather
clawhub install gog
```

### Step 3: SOUL.md 설정 (10분)
```markdown
나는 영진씨의 개인 비서.
- 할 일 요청 → Reminders에 추가
- 일정 질문 → Calendar 확인
- 매일 아침 8시 → 브리핑
```

### Step 4: 크론 설정 (5분)
아침 브리핑, 저녁 정리 크론 추가.

### Step 5: 테스트 & 튜닝 (계속)
실제 사용하며 프롬프트/스킬 개선.

---

## 출처
- [n8n: Telegram + Todoist 워크플로우](https://n8n.io/workflows/3052)
- [n8n: Multi-Modal AI Assistant](https://n8n.io/workflows/8793)
- [n8n: Telegram AI Agent](https://webspacekit.com/n8n-workflows/telegram-ai-assistant-with-google-services-integration/)
- [Reddit: Telegram AI Assistant (n8n)](https://www.reddit.com/r/n8n/comments/1nf7z8b/)
- [OpenClaw 스킬 문서](https://docs.openclaw.ai/tools/skills)
- [Todoist REST API v2](https://developer.todoist.com/rest/v2/)
- [Medium: Telegram AI Agent with n8n](https://medium.com/@msm05/ai-recruitment-workflow-optimization-7cb1d85c1538)
