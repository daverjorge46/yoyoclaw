# 부록 11: 스킬 개발 튜토리얼

## 처음부터 끝까지: 메모 스킬 만들기

이 튜토리얼에서는 간단한 "quick-memo" 스킬을 만든다. 텍스트 파일에 메모를 저장/조회/삭제하는 스킬이다.

---

## Step 1: 폴더 구조 생성

```bash
mkdir -p ~/.openclaw/skills/quick-memo
```

---

## Step 2: SKILL.md 작성

```bash
cat > ~/.openclaw/skills/quick-memo/SKILL.md << 'EOF'
---
name: quick-memo
description: 간단한 텍스트 메모를 저장, 조회, 검색, 삭제하는 스킬
metadata: {"openclaw":{"emoji":"📝"}}
---

## Quick Memo

텍스트 기반 메모 관리 스킬. `~/.openclaw/memos/` 폴더에 마크다운 파일로 저장한다.

### 사용법

#### 메모 추가
"메모 추가: {제목} - {내용}" 형식으로 요청하면:
1. `~/.openclaw/memos/{날짜}_{제목}.md` 파일 생성
2. 프론트매터에 날짜, 태그 포함
3. 저장 완료 확인

#### 메모 조회
"메모 목록", "오늘 메모" 등으로 요청하면:
1. `~/.openclaw/memos/` 폴더의 파일 목록 조회
2. 최근 순으로 정렬
3. 제목과 첫 줄 요약 표시

#### 메모 검색
"메모 검색: {키워드}" 형식으로 요청하면:
1. 모든 메모 파일에서 키워드 검색
2. 매칭된 파일과 관련 줄 표시

#### 메모 삭제
"메모 삭제: {제목}" 형식으로 요청하면:
1. 해당 파일 확인 후 삭제 확인 요청
2. 사용자 확인 후 삭제

### 메모 파일 형식

```markdown
---
title: "{제목}"
date: "{YYYY-MM-DD HH:mm}"
tags: [{태그1}, {태그2}]
---

{내용}
```

### 주의사항
- 삭제 전 반드시 사용자 확인
- 파일명에 특수문자 제거 (영문, 숫자, 하이픈만)
- 메모 폴더가 없으면 자동 생성

### 예시

사용자: "메모 추가: 회의록 - 오늘 팀 회의에서 다음 주 마감 확정"
→ `~/.openclaw/memos/2026-02-01_회의록.md` 생성

사용자: "오늘 메모 보여줘"
→ 오늘 날짜 파일 목록 + 요약 표시
EOF
```

---

## Step 3: 스킬 로드 확인

```bash
# 게이트웨이가 실행 중이면 스킬 워처가 자동 감지 (watch: true)
# 또는 게이트웨이 재시작
openclaw gateway restart

# 상태 확인
openclaw status --all
```

`available skills` 목록에 `quick-memo` 가 보이면 성공.

---

## Step 4: 테스트

텔레그램 또는 CLI에서:
1. "메모 추가: 테스트 - 스킬 테스트 메모입니다"
2. "메모 목록"
3. "메모 검색: 테스트"
4. "메모 삭제: 테스트"

각 단계에서 올바르게 동작하는지 확인.

---

## Step 5: 개선

### 게이팅 추가 (선택)
특정 환경에서만 동작하게 하려면:
```yaml
metadata: {"openclaw":{"emoji":"📝","requires":{"bins":["grep"]}}}
```

### 참고 파일 추가 (선택)
```bash
mkdir -p ~/.openclaw/skills/quick-memo/references
echo "# 메모 작성 가이드\n- 제목은 간결하게\n- 태그를 활용하여 분류" > ~/.openclaw/skills/quick-memo/references/guide.md
```

### config에서 활성화/비활성화
```json
{
  "skills": {
    "entries": {
      "quick-memo": { "enabled": true }
    }
  }
}
```

---

## SKILL.md 작성법 정리

### 필수 요소

```yaml
---
name: skill-name          # 영문 케밥케이스
description: 한 줄 설명     # 간결하고 명확하게
---

{사용법 지시문}
```

### 선택 요소

```yaml
---
name: skill-name
description: 한 줄 설명
metadata:
  {
    "openclaw":
      {
        "emoji": "📝",
        "requires": { "bins": ["tool"], "env": ["API_KEY"] },
        "primaryEnv": "API_KEY",
        "os": ["darwin", "linux"],
        "always": true,
        "install": [{ "id": "brew", "kind": "brew", "formula": "tool", "bins": ["tool"] }]
      }
  }
homepage: https://example.com
user-invocable: true
---
```

### 지시문 작성 팁

1. **명확한 사용법**: 사용자가 어떻게 호출하는지 구체적으로
2. **예시 포함**: 실제 사용 예시를 2~3개
3. **에러 핸들링**: 실패 시 어떻게 하는지
4. **주의사항**: 제약조건, 보안 고려
5. **{baseDir} 활용**: 참고 파일 경로 참조

---

## ClawHub 배포 (선택)

```bash
# 스킬 동기화
clawhub sync --all

# 또는 수동으로 설치 가능하게 공유
# GitHub에 올리고 clawhub install <slug>로 설치
```

---

## 스킬 개발 체크리스트

- [ ] `name`: 영문 케밥케이스
- [ ] `description`: 한 줄, 명확
- [ ] `metadata`: JSON 유효 (한 줄)
- [ ] `requires`: 정확한 의존성
- [ ] 지시문: 구체적 사용법
- [ ] 예시: 2~3개 이상
- [ ] 에러 핸들링 포함
- [ ] 보안 고려 (API 키 노출 방지)
- [ ] 스킬 워처 또는 게이트웨이 재시작으로 로드 확인
- [ ] 실제 동작 테스트 완료
