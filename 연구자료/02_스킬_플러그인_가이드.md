# OpenClaw 스킬 & 플러그인 완전 가이드

## 1. 개요

OpenClaw의 스킬 시스템은 AI 에이전트에 **도구(tool)**를 부여하는 모듈형 확장 체계다. 각 스킬은 `SKILL.md` 파일 하나로 정의되며, CLI 바이너리/API/스크립트를 감싸서 에이전트가 자연어 명령으로 외부 서비스를 제어할 수 있게 한다.

### 3단계 로딩 우선순위

| 우선순위 | 유형 | 경로 | 설명 |
|---------|------|------|------|
| 1 (최고) | **Workspace** | `<workspace>/skills/` | 프로젝트별 커스텀 스킬 |
| 2 | **Managed** | `~/.openclaw/skills/` | 사용자 설치/로컬 스킬 |
| 3 (최저) | **Bundled** | 설치 디렉토리 내 `skills/` | 기본 제공 52개 스킬 |

동일 이름의 스킬이 여러 계층에 존재하면 **높은 우선순위가 덮어쓴다**.

### 게이팅 시스템

스킬은 로드 시 자동 필터링된다:

- **os**: 운영체제 제한 (`darwin`, `linux`, `win32`)
- **requires.bins**: PATH에 필수 바이너리 존재 여부
- **requires.anyBins**: 나열된 바이너리 중 하나 이상 존재
- **requires.env**: 필수 환경변수
- **requires.config**: `openclaw.json` 내 설정값

조건을 충족하지 못하면 해당 스킬은 자동으로 비활성화된다.

---

## 2. 스킬 카테고리별 전체 목록 (52개)

### 2.1 개발 도구 (7개)

#### coding-agent
| 항목 | 내용 |
|------|------|
| 설명 | Codex CLI, Claude Code, OpenCode, Pi 등 코딩 에이전트를 백그라운드 프로세스로 실행 |
| 필수 바이너리 | `claude`, `codex`, `opencode`, `pi` (하나 이상) |
| 설정 | 해당 CLI 설치 후 자동 감지 |
| 사용 예시 | PTY 모드로 코딩 에이전트 실행, PR 리뷰 자동화, 병렬 git worktree 작업 |
| 추천도 | ★★★★★ |

#### github
| 항목 | 내용 |
|------|------|
| 설명 | `gh` CLI로 GitHub 이슈, PR, CI, API 쿼리 수행 |
| 필수 바이너리 | `gh` |
| 설치 | `brew install gh` |
| 사용 예시 | `gh pr list`, `gh issue create`, `gh run view`, `gh api graphql` |
| 추천도 | ★★★★★ |

#### skill-creator
| 항목 | 내용 |
|------|------|
| 설명 | 새로운 스킬을 설계하고 패키징하는 메타 스킬 |
| 필수 바이너리 | 없음 |
| 사용 예시 | 스킬 초기화, SKILL.md 작성, 스크립트/참조파일 구성, 패키징 |
| 추천도 | ★★★★☆ |

#### tmux
| 항목 | 내용 |
|------|------|
| 설명 | tmux 세션을 원격 제어하여 인터랙티브 CLI 실행 |
| 필수 바이너리 | `tmux` |
| OS | macOS, Linux |
| 사용 예시 | 격리된 소켓 세션 생성, 키 입력 전송, pane 캡처, REPL 스폰 |
| 추천도 | ★★★★☆ |

#### oracle
| 항목 | 내용 |
|------|------|
| 설명 | 프롬프트 + 파일을 번들링하여 외부 LLM(GPT-5.2 Pro 등)에 원샷 요청 |
| 필수 바이너리 | `oracle` |
| 설치 | `npm install -g @steipete/oracle` |
| 사용 예시 | `oracle --prompt "분석해줘" --file src/*.ts --engine browser` |
| 추천도 | ★★★★☆ |

#### clawhub
| 항목 | 내용 |
|------|------|
| 설명 | clawhub.com에서 스킬 검색, 설치, 업데이트, 퍼블리시 |
| 필수 바이너리 | `clawhub` |
| 설치 | `npm install -g clawhub` |
| 사용 예시 | `clawhub search weather`, `clawhub install skill-name`, `clawhub publish` |
| 추천도 | ★★★☆☆ |

#### model-usage
| 항목 | 내용 |
|------|------|
| 설명 | CodexBar CLI로 모델별 사용량/비용 요약 |
| 필수 바이너리 | `codexbar` |
| OS | macOS |
| 설치 | `brew install --cask codexbar` |
| 사용 예시 | `codexbar usage --current`, `codexbar usage --all --json` |
| 추천도 | ★★★☆☆ |

---

### 2.2 생산성 (8개)

#### apple-notes
| 항목 | 내용 |
|------|------|
| 설명 | `memo` CLI로 Apple Notes 관리 (생성, 검색, 편집, 삭제, 내보내기) |
| 필수 바이너리 | `memo` |
| OS | macOS |
| 설치 | `brew install antoniorodr/memo/memo` |
| 사용 예시 | `memo notes list`, `memo notes create --title "제목"`, `memo notes export --format md` |
| 추천도 | ★★★★☆ |

#### apple-reminders
| 항목 | 내용 |
|------|------|
| 설명 | `remindctl` CLI로 Apple Reminders 관리 (조회, 추가, 완료, 삭제) |
| 필수 바이너리 | `remindctl` |
| OS | macOS |
| 설치 | `brew install steipete/tap/remindctl` |
| 사용 예시 | `remindctl today`, `remindctl add "우유 사기" --due tomorrow`, `remindctl complete <id>` |
| 추천도 | ★★★★★ |

#### bear-notes
| 항목 | 내용 |
|------|------|
| 설명 | `grizzly` CLI로 Bear 노트 생성, 검색, 관리 |
| 필수 바이너리 | `grizzly` |
| OS | macOS |
| 설치 | `go install github.com/tylerwince/grizzly/cmd/grizzly@latest` |
| 사용 예시 | 노트 생성, 태그 검색, 텍스트 추가 |
| 추천도 | ★★★☆☆ |

#### notion
| 항목 | 내용 |
|------|------|
| 설명 | Notion API로 페이지, 데이터베이스, 블록 관리 |
| 필수 환경변수 | `NOTION_API_KEY` |
| 설정 | notion.so/my-integrations에서 통합 생성 후 API 키 저장 |
| 사용 예시 | 페이지 검색/생성, 데이터소스 쿼리, 블록 추가/수정 |
| 추천도 | ★★★★★ |

#### obsidian
| 항목 | 내용 |
|------|------|
| 설명 | Obsidian vault (Markdown 노트)와 `obsidian-cli` 자동화 |
| 필수 바이너리 | `obsidian-cli` |
| 설치 | `brew install yakitrak/yakitrak/obsidian-cli` |
| 사용 예시 | vault 검색, 노트 생성/이동, wikilink 자동 업데이트 |
| 추천도 | ★★★★☆ |

#### things-mac
| 항목 | 내용 |
|------|------|
| 설명 | Things 3 할일 관리 (`things` CLI: URL scheme 기반 쓰기 + DB 직접 읽기) |
| 필수 바이너리 | `things` |
| OS | macOS |
| 설치 | `go install github.com/ossianhempel/things3-cli/cmd/things@latest` |
| 사용 예시 | `things today`, `things add "할일" --when today`, `things search "키워드"` |
| 추천도 | ★★★★☆ |

#### trello
| 항목 | 내용 |
|------|------|
| 설명 | Trello REST API로 보드, 리스트, 카드 CRUD |
| 필수 바이너리 | `jq` |
| 필수 환경변수 | `TRELLO_API_KEY`, `TRELLO_TOKEN` |
| 사용 예시 | 보드 조회, 카드 생성/이동/아카이브, 코멘트 추가 |
| 추천도 | ★★★☆☆ |

#### gog
| 항목 | 내용 |
|------|------|
| 설명 | Google Workspace CLI (Gmail, Calendar, Drive, Contacts, Sheets, Docs) |
| 필수 바이너리 | `gog` |
| 설치 | `brew install steipete/tap/gogcli` |
| 설정 | OAuth: `gog auth credentials <client_secret.json>` + `gog auth add <email>` |
| 사용 예시 | 메일 검색/발송, 캘린더 이벤트 생성, Sheets 읽기/쓰기, Docs 내보내기 |
| 추천도 | ★★★★★ |

---

### 2.3 메시징 (7개)

#### slack
| 항목 | 내용 |
|------|------|
| 설명 | Slack 채널/DM에서 메시지 전송, 리액션, 핀, 검색 등 |
| 필수 설정 | `channels.slack` (openclaw.json) |
| 사용 예시 | 메시지 전송/편집/삭제, 리액션 추가, 핀 고정, 멤버 정보 조회 |
| 추천도 | ★★★★★ |

#### discord
| 항목 | 내용 |
|------|------|
| 설명 | Discord 채널/DM 전체 제어 (메시지, 리액션, 스레드, 폴, 채널 관리, 모더레이션) |
| 필수 설정 | `channels.discord` (openclaw.json) |
| 사용 예시 | 메시지 전송, 스티커/이모지 업로드, 투표 생성, 채널 생성/삭제 |
| 추천도 | ★★★★★ |

#### imsg
| 항목 | 내용 |
|------|------|
| 설명 | iMessage/SMS CLI로 채팅 목록, 히스토리, 감시, 전송 |
| 필수 바이너리 | `imsg` |
| OS | macOS |
| 설치 | `brew install imsg` |
| 사용 예시 | `imsg chats`, `imsg history <chat>`, `imsg send <number> "메시지"` |
| 추천도 | ★★★★☆ |

#### bluebubbles
| 항목 | 내용 |
|------|------|
| 설명 | BlueBubbles 앱을 통한 iMessage 외부 채널 플러그인 구축/업데이트 |
| 필수 설정 | BlueBubbles 서버 + REST API |
| 사용 예시 | REST 전송/프로브, 웹훅 인바운드, 리액션, 타이핑 표시 |
| 추천도 | ★★★☆☆ |

#### himalaya
| 항목 | 내용 |
|------|------|
| 설명 | IMAP/SMTP 기반 이메일 CLI 관리 |
| 필수 바이너리 | `himalaya` |
| 설치 | `brew install himalaya` |
| 사용 예시 | 폴더/메일 조회, 검색, 읽기, 답장, 전달, 첨부파일 관리 |
| 추천도 | ★★★★☆ |

#### wacli
| 항목 | 내용 |
|------|------|
| 설명 | WhatsApp 메시지 전송 및 히스토리 검색/동기화 |
| 필수 바이너리 | `wacli` |
| 설치 | `brew install wacli` 또는 Go 설치 |
| 사용 예시 | QR 인증, 히스토리 동기화, 텍스트/파일 전송, 그룹 지원 |
| 추천도 | ★★★☆☆ |

#### voice-call
| 항목 | 내용 |
|------|------|
| 설명 | OpenClaw voice-call 플러그인으로 음성 통화 시작 |
| 필수 설정 | `plugins.entries.voice-call.enabled` |
| 사용 예시 | 통화 시작/종료, 음성 메시지 전달, 상태 확인 (Twilio/Telnyx/Plivo 지원) |
| 추천도 | ★★★☆☆ |

---

### 2.4 AI 서비스 (7개)

#### gemini
| 항목 | 내용 |
|------|------|
| 설명 | Gemini CLI로 원샷 Q&A, 요약, 생성 |
| 필수 바이너리 | `gemini` |
| 설치 | `brew install gemini-cli` |
| 사용 예시 | `gemini "요약해줘"`, `gemini --model gemini-3-pro "분석해줘"` |
| 추천도 | ★★★★☆ |

#### nano-banana-pro
| 항목 | 내용 |
|------|------|
| 설명 | Gemini 3 Pro Image로 이미지 생성/편집 (최대 4K, 멀티이미지 합성) |
| 필수 바이너리 | `uv` |
| 필수 환경변수 | `GEMINI_API_KEY` |
| 설치 | `brew install uv` |
| 사용 예시 | `uv run generate_image.py --prompt "설명" --resolution 2K` |
| 추천도 | ★★★★★ |

#### openai-image-gen
| 항목 | 내용 |
|------|------|
| 설명 | OpenAI Images API로 배치 이미지 생성 + HTML 갤러리 |
| 필수 바이너리 | `python3` |
| 필수 환경변수 | `OPENAI_API_KEY` |
| 사용 예시 | `python3 gen.py --count 4 --model gpt-image-1 --quality high` |
| 추천도 | ★★★★☆ |

#### openai-whisper
| 항목 | 내용 |
|------|------|
| 설명 | 로컬 Whisper로 음성-텍스트 변환 (API 키 불필요) |
| 필수 바이너리 | `whisper` |
| 설치 | `brew install openai-whisper` |
| 사용 예시 | `whisper audio.mp3 --model medium --output_format txt` |
| 추천도 | ★★★★☆ |

#### openai-whisper-api
| 항목 | 내용 |
|------|------|
| 설명 | OpenAI Whisper API로 클라우드 음성 변환 |
| 필수 바이너리 | `curl` |
| 필수 환경변수 | `OPENAI_API_KEY` |
| 사용 예시 | `transcribe.sh audio.m4a --language en --prompt "화자: 홍길동"` |
| 추천도 | ★★★☆☆ |

#### summarize
| 항목 | 내용 |
|------|------|
| 설명 | URL, 로컬 파일, YouTube 링크를 요약/트랜스크립트 추출 |
| 필수 바이너리 | `summarize` |
| 설치 | `brew install steipete/tap/summarize` |
| 사용 예시 | `summarize "https://example.com" --model google/gemini-3-flash-preview --length medium` |
| 추천도 | ★★★★★ |

#### oracle
> 개발 도구 섹션 참조 (외부 LLM 프롬프트 번들링)

---

### 2.5 스마트홈 (5개)

#### openhue
| 항목 | 내용 |
|------|------|
| 설명 | Philips Hue 조명/씬 제어 |
| 필수 바이너리 | `openhue` |
| 설치 | `brew install openhue/cli/openhue-cli` |
| 사용 예시 | `openhue set light <id> --on --brightness 50 --rgb #3399FF` |
| 추천도 | ★★★★☆ |

#### sonoscli
| 항목 | 내용 |
|------|------|
| 설명 | Sonos 스피커 제어 (재생, 볼륨, 그룹, 즐겨찾기) |
| 필수 바이너리 | `sonos` |
| 설치 | `go install github.com/steipete/sonoscli/cmd/sonos@latest` |
| 사용 예시 | `sonos discover`, `sonos play --name "Kitchen"`, `sonos volume set 15` |
| 추천도 | ★★★★☆ |

#### eightctl
| 항목 | 내용 |
|------|------|
| 설명 | Eight Sleep 포드 제어 (온도, 알람, 스케줄, 오디오) |
| 필수 바이너리 | `eightctl` |
| 설치 | `go install github.com/steipete/eightctl/cmd/eightctl@latest` |
| 사용 예시 | `eightctl status`, `eightctl temp 20`, `eightctl alarm list` |
| 추천도 | ★★★☆☆ |

#### blucli
| 항목 | 내용 |
|------|------|
| 설명 | BluOS/Bluesound/NAD 플레이어 제어 |
| 필수 바이너리 | `blu` |
| 설치 | `go install github.com/steipete/blucli/cmd/blu@latest` |
| 사용 예시 | `blu devices`, `blu play`, `blu volume set 15`, `blu tunein search "jazz"` |
| 추천도 | ★★★☆☆ |

#### spotify-player
| 항목 | 내용 |
|------|------|
| 설명 | 터미널 Spotify 재생/검색 (spogo 또는 spotify_player) |
| 필수 바이너리 | `spogo` 또는 `spotify_player` (하나 이상) |
| 설치 | `brew install spogo` 또는 `brew install spotify_player` |
| 설정 | Spotify Premium 필수, `spogo auth import --browser chrome` |
| 사용 예시 | 검색, 재생/일시정지, 디바이스 관리 |
| 추천도 | ★★★★☆ |

---

### 2.6 미디어 (6개)

#### camsnap
| 항목 | 내용 |
|------|------|
| 설명 | RTSP/ONVIF 카메라 스냅샷, 클립, 모션 감지 |
| 필수 바이너리 | `camsnap` (+ `ffmpeg`) |
| 설치 | `brew install steipete/tap/camsnap` |
| 사용 예시 | `camsnap snap kitchen --out shot.jpg`, `camsnap clip kitchen --dur 5s` |
| 추천도 | ★★★☆☆ |

#### gifgrep
| 항목 | 내용 |
|------|------|
| 설명 | GIF 검색 (Tenor/Giphy), 다운로드, 스틸/시트 추출 |
| 필수 바이너리 | `gifgrep` |
| 설치 | `brew install gifgrep` |
| 사용 예시 | `gifgrep search "happy dance"`, TUI 미리보기, 스틸 추출 |
| 추천도 | ★★★☆☆ |

#### nano-pdf
| 항목 | 내용 |
|------|------|
| 설명 | 자연어 명령으로 PDF 편집 |
| 필수 바이너리 | `nano-pdf` |
| 설치 | `uv tool install nano-pdf` |
| 사용 예시 | `nano-pdf edit deck.pdf 1 "제목을 'Q3 결과'로 변경"` |
| 추천도 | ★★★★☆ |

#### songsee
| 항목 | 내용 |
|------|------|
| 설명 | 오디오 스펙트로그램/시각화 생성 (mel, chroma, MFCC 등) |
| 필수 바이너리 | `songsee` |
| 설치 | `brew install songsee` |
| 사용 예시 | 스펙트로그램, 멀티패널 시각화, 시간 슬라이스 |
| 추천도 | ★★☆☆☆ |

#### video-frames
| 항목 | 내용 |
|------|------|
| 설명 | ffmpeg로 비디오에서 프레임/클립 추출 |
| 필수 바이너리 | `ffmpeg` |
| 설치 | `brew install ffmpeg` |
| 사용 예시 | 첫 프레임 추출, 특정 타임스탬프 프레임 추출 |
| 추천도 | ★★★☆☆ |

#### peekaboo
| 항목 | 내용 |
|------|------|
| 설명 | macOS UI 캡처 및 자동화 (스크린샷, 클릭, 드래그, 타이핑 등) |
| 필수 바이너리 | `peekaboo` |
| OS | macOS |
| 설치 | `brew install peekaboo` |
| 사용 예시 | 화면 캡처/분석, UI 클릭, 핫키, 앱/윈도우/메뉴 관리 |
| 추천도 | ★★★★☆ |

---

### 2.7 위치/장소 (2개)

#### goplaces
| 항목 | 내용 |
|------|------|
| 설명 | Google Places API (New)로 장소 검색, 상세 정보, 리뷰 |
| 필수 바이너리 | `goplaces` |
| 필수 환경변수 | `GOOGLE_PLACES_API_KEY` |
| 설치 | `brew install goplaces` |
| 사용 예시 | `goplaces search "카페" --bias lat,lng`, `goplaces details <id>` |
| 추천도 | ★★★☆☆ |

#### local-places
| 항목 | 내용 |
|------|------|
| 설명 | Google Places API 프록시로 근처 장소 검색 |
| 필수 바이너리 | `uv` |
| 필수 환경변수 | `GOOGLE_PLACES_API_KEY` |
| 사용 예시 | 위치 해석, 타입/가격/평점 필터 검색, 페이지네이션 |
| 추천도 | ★★☆☆☆ |

---

### 2.8 서비스 (2개)

#### food-order
| 항목 | 내용 |
|------|------|
| 설명 | Foodora 재주문 + ETA/상태 추적 |
| 필수 바이너리 | `ordercli` |
| 설치 | `go install github.com/steipete/ordercli/cmd/ordercli@latest` |
| 사용 예시 | 주문 이력 조회, 재주문 (확인 필수), ETA 추적 |
| 추천도 | ★★★☆☆ |

#### ordercli
| 항목 | 내용 |
|------|------|
| 설명 | Foodora 전용 CLI (주문 이력, 활성 주문 상태 확인) |
| 필수 바이너리 | `ordercli` |
| 설치 | `brew install ordercli` 또는 Go 설치 |
| 사용 예시 | 주문 확인, 상태 추적, 재주문, 브라우저 로그인 |
| 추천도 | ★★☆☆☆ |

---

### 2.9 소셜 (1개)

#### bird
| 항목 | 내용 |
|------|------|
| 설명 | X/Twitter CLI (읽기, 검색, 게시, 미디어 업로드) |
| 필수 바이너리 | `bird` |
| 설치 | `npm install -g bird` 또는 `brew install bird` |
| 사용 예시 | 타임라인 읽기, 트윗 검색, 포스팅, 좋아요/북마크, 미디어 업로드 |
| 추천도 | ★★★★☆ |

---

### 2.10 보안 (1개)

#### 1password
| 항목 | 내용 |
|------|------|
| 설명 | 1Password CLI (`op`)로 시크릿 관리 (읽기, 주입, 실행) |
| 필수 바이너리 | `op` |
| 설치 | `brew install 1password-cli` |
| 설정 | 데스크톱 앱 통합 활성화, tmux 세션에서 `op` 사용 |
| 사용 예시 | `op run -- command`, `op inject`, `op item get "name"` |
| 추천도 | ★★★★★ |

---

### 2.11 기타 (6개)

#### blogwatcher
| 항목 | 내용 |
|------|------|
| 설명 | 블로그/RSS/Atom 피드 모니터링 |
| 필수 바이너리 | `blogwatcher` |
| 설치 | `go install github.com/Hyaxia/blogwatcher/cmd/blogwatcher@latest` |
| 사용 예시 | 블로그 추가/제거, 스캔, 아티클 목록, 읽음 표시 |
| 추천도 | ★★★☆☆ |

#### canvas
| 항목 | 내용 |
|------|------|
| 설명 | 연결된 OpenClaw 노드(Mac/iOS/Android)에 HTML 콘텐츠 표시 |
| 필수 설정 | 없음 (내장) |
| 사용 예시 | HTML 프레젠테이션, JS 실행, 스냅샷, 라이브 리로드 |
| 추천도 | ★★★☆☆ |

#### mcporter
| 항목 | 내용 |
|------|------|
| 설명 | MCP 서버 목록 조회, 설정, 인증, 도구 직접 호출 |
| 필수 바이너리 | `mcporter` |
| 설치 | `npm install -g mcporter` |
| 사용 예시 | MCP 서버 목록, 도구 호출, OAuth 인증, 코드 생성 |
| 추천도 | ★★★☆☆ |

#### sag
| 항목 | 내용 |
|------|------|
| 설명 | ElevenLabs TTS (macOS `say` 스타일 UX) |
| 필수 바이너리 | `sag` |
| 필수 환경변수 | `ELEVENLABS_API_KEY` (또는 `SAG_API_KEY`) |
| 설치 | `brew install sag` |
| 사용 예시 | 다양한 TTS 모델, SSML, 발음 가이드, 오디오 태그 |
| 추천도 | ★★★☆☆ |

#### session-logs
| 항목 | 내용 |
|------|------|
| 설명 | 자신의 세션 로그(JSONL) 검색 및 분석 |
| 필수 바이너리 | `jq`, `rg` |
| 사용 예시 | 키워드 검색, 비용 계산, 도구 사용량 분석, 대화 전체 복원 |
| 추천도 | ★★★☆☆ |

#### sherpa-onnx-tts
| 항목 | 내용 |
|------|------|
| 설명 | 로컬 TTS (오프라인, Sherpa-ONNX + Piper 모델) |
| 필수 환경변수 | `SHERPA_ONNX_RUNTIME_DIR`, `SHERPA_ONNX_MODEL_DIR` |
| 설치 | 런타임 + 모델 다운로드 후 환경변수 설정 |
| 사용 예시 | 오프라인 음성 합성, 다양한 보이스 모델, WAV/MP3 출력 |
| 추천도 | ★★☆☆☆ |

#### weather
| 항목 | 내용 |
|------|------|
| 설명 | 현재 날씨 및 예보 조회 (API 키 불필요) |
| 필수 바이너리 | `curl` |
| 사용 예시 | `curl "wttr.in/Seoul?format=%c+%t+%h"`, Open-Meteo JSON 폴백 |
| 추천도 | ★★★★★ |

---

## 3. 초보자 추천 TOP 20

유용성, 설정 난이도, 활용 범위를 기준으로 순위를 매겼다.

| 순위 | 스킬 | 카테고리 | 설정 난이도 | 추천 이유 |
|------|------|---------|------------|----------|
| 1 | **weather** | 기타 | 매우 쉬움 | 설정 없이 바로 사용, 일상 유용 |
| 2 | **github** | 개발 | 쉬움 | 개발자 필수, gh CLI만 설치 |
| 3 | **apple-reminders** | 생산성 | 쉬움 | macOS 유저 할일 관리 핵심 |
| 4 | **summarize** | AI | 쉬움 | URL/파일/YouTube 요약 만능 |
| 5 | **slack** | 메시징 | 보통 | 팀 커뮤니케이션 자동화 |
| 6 | **discord** | 메시징 | 보통 | 커뮤니티 운영 필수 |
| 7 | **notion** | 생산성 | 보통 | 지식 관리 + 프로젝트 관리 |
| 8 | **1password** | 보안 | 보통 | 시크릿 안전 관리 |
| 9 | **gog** | 생산성 | 보통 | Gmail+Calendar+Drive 올인원 |
| 10 | **coding-agent** | 개발 | 보통 | AI 코딩 에이전트 오케스트레이션 |
| 11 | **nano-banana-pro** | AI | 보통 | 고품질 이미지 생성/편집 |
| 12 | **apple-notes** | 생산성 | 쉬움 | macOS 노트 자동화 |
| 13 | **obsidian** | 생산성 | 쉬움 | Markdown 지식베이스 관리 |
| 14 | **gemini** | AI | 쉬움 | 빠른 원샷 AI 질의 |
| 15 | **spotify-player** | 스마트홈 | 보통 | 음악 재생 제어 |
| 16 | **nano-pdf** | 미디어 | 쉬움 | 자연어 PDF 편집 |
| 17 | **openai-whisper** | AI | 쉬움 | 로컬 음성 인식 (무료) |
| 18 | **bird** | 소셜 | 보통 | X/Twitter 자동화 |
| 19 | **himalaya** | 메시징 | 보통 | IMAP 이메일 CLI 관리 |
| 20 | **openhue** | 스마트홈 | 보통 | Hue 조명 AI 제어 |

---

## 4. 스킬 설치/활성화 가이드

### 4.1 Bundled vs Managed vs Workspace

```
Workspace (프로젝트별)     ← 최고 우선순위
  <project>/skills/my-skill/SKILL.md

Managed (사용자 글로벌)
  ~/.openclaw/skills/my-skill/SKILL.md

Bundled (기본 제공)        ← 최저 우선순위
  <install-dir>/skills/weather/SKILL.md
```

### 4.2 설정 파일 (`~/.openclaw/openclaw.json`)

```json
{
  "skills": {
    "allowBundled": ["weather", "github", "slack"],
    "load": {
      "extraDirs": ["~/my-skills"],
      "watch": true,
      "watchDebounceMs": 250
    },
    "install": {
      "preferBrew": true,
      "nodeManager": "npm"
    },
    "entries": {
      "notion": {
        "enabled": true,
        "env": { "NOTION_API_KEY": "secret_xxx" }
      },
      "nano-banana-pro": {
        "enabled": true,
        "apiKey": "AIza..."
      }
    }
  }
}
```

### 4.3 주요 설정 옵션

| 옵션 | 설명 |
|------|------|
| `allowBundled` | 활성화할 번들 스킬 화이트리스트 (미설정시 전체 활성) |
| `load.extraDirs` | 추가 스킬 디렉토리 경로 |
| `load.watch` | 스킬 파일 변경 감시 (핫 리로드) |
| `entries.<name>.enabled` | 개별 스킬 활성/비활성 |
| `entries.<name>.env` | 스킬별 환경변수 오버라이드 |
| `entries.<name>.apiKey` | 스킬별 API 키 |
| `entries.<name>.config` | 스킬별 커스텀 설정 |

### 4.4 게이팅 조건 확인

스킬이 로드되지 않을 때 체크리스트:

1. **바이너리 확인**: `which <binary>` 로 PATH에 있는지 확인
2. **환경변수 확인**: `echo $VAR_NAME`
3. **OS 확인**: macOS 전용 스킬은 Linux에서 비활성
4. **설정 확인**: `openclaw.json`에 필요한 config 경로 존재 여부

---

## 5. 스킬 조합 시나리오

### 아침 루틴

| 단계 | 스킬 | 동작 |
|------|------|------|
| 1 | weather | 오늘 날씨 확인 |
| 2 | apple-reminders | 오늘 할일 목록 조회 |
| 3 | gog | Gmail 미읽은 메일 + 오늘 캘린더 확인 |
| 4 | spotify-player | 출근 플레이리스트 재생 |

**예시 프롬프트**: "오늘 아침 브리핑 해줘 - 날씨, 할일, 메일, 일정 알려주고 음악 틀어줘"

### 업무 자동화

| 단계 | 스킬 | 동작 |
|------|------|------|
| 1 | github | PR 목록 및 CI 상태 확인 |
| 2 | notion | 프로젝트 보드 업데이트 |
| 3 | slack | 팀 채널에 상태 공유 |
| 4 | coding-agent | 코드 리뷰 자동화 |

**예시 프롬프트**: "오늘 PR 상태 확인하고, Notion에 진행률 업데이트하고, Slack에 보고해줘"

### 콘텐츠 제작

| 단계 | 스킬 | 동작 |
|------|------|------|
| 1 | summarize | 참고 URL/영상 요약 |
| 2 | nano-banana-pro | 썸네일/일러스트 생성 |
| 3 | nano-pdf | PDF 보고서 편집 |
| 4 | bird | X/Twitter에 게시 |

**예시 프롬프트**: "이 YouTube 영상 요약하고, 관련 이미지 만들어서, 트위터에 스레드로 올려줘"

### 스마트홈 제어

| 단계 | 스킬 | 동작 |
|------|------|------|
| 1 | openhue | 조명을 따뜻한 톤으로 |
| 2 | sonoscli | 거실 스피커에 재즈 재생 |
| 3 | eightctl | 침대 온도 22도 설정 |

**예시 프롬프트**: "저녁 모드로 전환해줘 - 조명 어둡게, 재즈 틀어주고, 침대 온도 맞춰줘"

### 보안 관리

| 단계 | 스킬 | 동작 |
|------|------|------|
| 1 | 1password | 시크릿/토큰 안전하게 조회 |
| 2 | github | 시크릿을 사용해 API 작업 수행 |

**예시 프롬프트**: "1Password에서 GitHub 토큰 가져와서 그 레포에 시크릿 설정해줘"

---

## 6. 커스텀 스킬 개발 가이드

### 6.1 SKILL.md 구조

```yaml
---
name: my-skill          # 소문자, 하이픈만
description: >-         # 에이전트가 스킬을 선택하는 주요 트리거
  이 스킬이 언제 사용되는지 설명
homepage: https://...    # 선택
user-invocable: /my-cmd  # 선택: 사용자가 직접 호출 가능한 슬래시 커맨드
metadata:
  openclaw:
    emoji: "🔧"
    os: [darwin, linux]
    requires:
      bins: [my-binary]
      env: [MY_API_KEY]
      config: [some.config.path]
    install:
      - id: brew
        kind: brew
        formula: my-tap/my-binary
        bins: [my-binary]
        label: Install my-binary (brew)
---

# My Skill 사용 방법

여기에 에이전트가 읽을 상세 지침을 작성한다.
```

### 6.2 디렉토리 구조

```
my-skill/
├── SKILL.md              # 필수: 프론트매터 + 지침
├── scripts/              # 선택: 실행 가능한 스크립트
│   ├── init_skill.py
│   └── helper.sh
├── references/           # 선택: 문서/API 레퍼런스
│   └── api-docs.md
└── assets/               # 선택: 템플릿, 이미지, 보일러플레이트
    └── template.html
```

### 6.3 설계 원칙

**Progressive Disclosure (점진적 공개)**:
1. `metadata` → 게이팅 (로드할지 결정)
2. `description` → 트리거링 (사용할지 결정)
3. SKILL.md body → 상세 지침 (어떻게 사용할지)
4. `references/` → 심화 참조 (필요할 때만)

**자유도 설정**:
- **높음**: 창의적 작업 (이미지 생성, 글쓰기)
- **중간**: 구조화된 작업 (API 호출, 데이터 처리)
- **낮음**: 안전 중요 작업 (결제, 삭제, 시크릿)

**간결함**: context window가 공유되므로 불필요한 내용을 최소화한다.

### 6.4 스킬 생성 워크플로우

1. **이해**: 구체적인 사용 예시를 먼저 정의
2. **계획**: 재사용 가능한 스크립트, 참조파일, 에셋 식별
3. **초기화**: `scripts/init_skill.py <name> --path <dir>`
4. **작성**: SKILL.md 편집 + 리소스 추가
5. **패키징**: `scripts/package_skill.py <path>`
6. **반복**: 실제 사용 기반으로 개선

### 6.5 배포

- **로컬**: `~/.openclaw/skills/`에 복사
- **워크스페이스**: 프로젝트 `skills/`에 추가
- **커뮤니티**: `clawhub publish`로 clawhub.com에 퍼블리시

---

## 7. 주요 연동 상세

### 7.1 1Password 연동

```bash
# CLI 설치
brew install 1password-cli

# 데스크톱 앱 통합 활성화 (Settings > Developer > CLI Integration)

# tmux 세션에서 사용 (보안 권장)
tmux new-session -s secure
op signin
op item get "GitHub Token" --fields password
op run --env-file=.env -- ./my-script.sh
```

핵심 규칙: 시크릿을 로그에 노출하지 않기. `op run`/`op inject` 선호.

### 7.2 Apple Notes/Obsidian

**Apple Notes** (`memo`):
```bash
memo notes list --folder "업무"
memo notes create --title "회의록" --body "내용"
memo notes search "키워드"
memo notes export --id <id> --format md
```

**Obsidian** (`obsidian-cli`):
```bash
obsidian-cli set-default "My Vault"
obsidian-cli search "검색어"
obsidian-cli create "새 노트" --content "내용"
# 또는 직접 .md 파일 편집 (선호)
```

### 7.3 GitHub + Notion 워크플로우

```bash
# GitHub에서 이슈 목록 가져오기
gh issue list --state open --json number,title,labels

# Notion 데이터소스에 반영
# (Notion API로 페이지 생성/업데이트)
```

### 7.4 이미지/미디어 스킬

| 스킬 | 용도 | API 키 |
|------|------|--------|
| nano-banana-pro | Gemini 이미지 생성/편집 (최대 4K) | GEMINI_API_KEY |
| openai-image-gen | OpenAI 배치 이미지 생성 + 갤러리 | OPENAI_API_KEY |
| openai-whisper | 로컬 음성 인식 | 불필요 |
| openai-whisper-api | 클라우드 음성 인식 | OPENAI_API_KEY |
| summarize | URL/파일/YouTube 요약 | 다양한 LLM 키 |
| video-frames | 비디오 프레임 추출 | 불필요 |

---

## 8. 확장 플러그인 목록 (29개)

확장 플러그인은 `extensions/` 디렉토리에 위치하며, 주로 메시징 채널 연동과 핵심 기능을 담당한다. 모든 플러그인은 버전 `2026.1.29`이며 TypeScript (`index.ts`)로 작성되었다.

### 8.1 메시징 채널 (17개)

| 플러그인 | 패키지명 | 설명 |
|---------|---------|------|
| **slack** | @openclaw/slack | Slack 채널 통합 |
| **discord** | @openclaw/discord | Discord 채널 통합 |
| **telegram** | @openclaw/telegram | Telegram 봇 통합 |
| **signal** | @openclaw/signal | Signal 암호화 메시징 |
| **whatsapp** | @openclaw/whatsapp | WhatsApp Cloud API |
| **imessage** | @openclaw/imessage | 네이티브 iMessage |
| **bluebubbles** | @openclaw/bluebubbles | BlueBubbles 앱 경유 iMessage |
| **googlechat** | @openclaw/googlechat | Google Workspace Chat (HTTP 웹훅) |
| **msteams** | @openclaw/msteams | Microsoft Teams (Bot Framework) |
| **line** | @openclaw/line | LINE Messaging API (아시아 태평양) |
| **matrix** | @openclaw/matrix | Matrix/Element 프로토콜 (Bot SDK) |
| **mattermost** | @openclaw/mattermost | 셀프호스팅 Mattermost |
| **nextcloud-talk** | @openclaw/nextcloud-talk | Nextcloud Talk (셀프호스팅 웹훅) |
| **nostr** | @openclaw/nostr | Nostr NIP-04 암호화 DM (탈중앙화) |
| **tlon** | @openclaw/tlon | Tlon/Urbit 메시징 (탈중앙화) |
| **twitch** | @openclaw/twitch | Twitch 채팅 (Twurple API) |
| **zalo** | @openclaw/zalo | Zalo OA 채널 (베트남) |

### 8.2 인증 (3개)

| 플러그인 | 설명 |
|---------|------|
| **google-antigravity-auth** | Google Antigravity OAuth provider |
| **google-gemini-cli-auth** | Gemini CLI OAuth provider |
| **qwen-portal-auth** | Qwen Portal OAuth provider |

### 8.3 코어 컴포넌트 (6개)

| 플러그인 | 설명 |
|---------|------|
| **voice-call** | WebSocket 기반 음성 통화 (Twilio/Telnyx/Plivo) |
| **memory-core** | 핵심 메모리 검색 |
| **memory-lancedb** | LanceDB + OpenAI 기반 장기 기억 (자동 회상/캡처) |
| **llm-task** | JSON-only LLM 태스크 실행 |
| **lobster** | 타입드 파이프라인 + 재개 가능한 승인 워크플로우 |
| **diagnostics-otel** | OpenTelemetry 진단 내보내기 |

### 8.4 기타 (3개)

| 플러그인 | 설명 |
|---------|------|
| **copilot-proxy** | Copilot Proxy provider |
| **open-prose** | OpenProse VM 스킬 팩 (슬래시 커맨드 + 텔레메트리) |
| **zalouser** | Zalo 개인 계정 (QR 로그인) |

### 8.5 Plugin SDK 주요 API

`src/plugin-sdk/index.ts`에서 제공하는 핵심 타입:

| 카테고리 | 주요 export |
|---------|------------|
| 채널 타입 | Discord, iMessage, Slack, Telegram, Signal, WhatsApp, BlueBubbles, Google Chat, MS Teams, LINE |
| 채널 어댑터 | Auth, Config, Command, Directory, Gateway, Group, Messaging, Outbound, Resolver, Security, Setup, Status, Streaming, Threading, Pairing, Mention 등 |
| 코어 타입 | `ChannelAgentTool`, `ChannelCapabilities`, `ChannelMeta`, `ChannelId` |
| 플러그인 런타임 | `OpenClawPluginApi`, `OpenClawPluginService`, `PluginRuntime` |
| 유틸리티 | 계정 해석, 디렉토리 목록, allowlist/mention 게이팅, 타이핑 콜백, 미디어 유틸 |

---

## 부록: 스킬 요구사항 빠른 참조

| 스킬 | 바이너리 | 환경변수 | OS | 설치 방법 |
|------|---------|---------|-----|----------|
| 1password | op | - | 전체 | brew |
| apple-notes | memo | - | macOS | brew |
| apple-reminders | remindctl | - | macOS | brew |
| bear-notes | grizzly | - | macOS | go |
| bird | bird | - | 전체 | npm/brew |
| blogwatcher | blogwatcher | - | 전체 | go |
| bluebubbles | - | - | 전체 | config |
| blucli | blu | - | 전체 | go |
| camsnap | camsnap, ffmpeg | - | 전체 | brew |
| canvas | - | - | 전체 | 내장 |
| clawhub | clawhub | - | 전체 | npm |
| coding-agent | claude/codex/opencode/pi | - | 전체 | 각각 설치 |
| discord | - | - | 전체 | config |
| eightctl | eightctl | EIGHTCTL_EMAIL/PASSWORD | 전체 | go |
| food-order | ordercli | - | 전체 | go |
| gemini | gemini | - | 전체 | brew |
| gifgrep | gifgrep | - | 전체 | brew/go |
| github | gh | - | 전체 | brew |
| gog | gog | - | 전체 | brew |
| goplaces | goplaces | GOOGLE_PLACES_API_KEY | 전체 | brew |
| himalaya | himalaya | - | 전체 | brew |
| imsg | imsg | - | macOS | brew |
| local-places | uv | GOOGLE_PLACES_API_KEY | 전체 | python |
| mcporter | mcporter | - | 전체 | npm |
| model-usage | codexbar | - | macOS | brew-cask |
| nano-banana-pro | uv | GEMINI_API_KEY | 전체 | brew |
| nano-pdf | nano-pdf | - | 전체 | uv |
| notion | - | NOTION_API_KEY | 전체 | env |
| obsidian | obsidian-cli | - | 전체 | brew |
| openai-image-gen | python3 | OPENAI_API_KEY | 전체 | brew |
| openai-whisper | whisper | - | 전체 | brew |
| openai-whisper-api | curl | OPENAI_API_KEY | 전체 | 내장 |
| openhue | openhue | - | 전체 | brew |
| oracle | oracle | - | 전체 | npm |
| ordercli | ordercli | - | 전체 | brew/go |
| peekaboo | peekaboo | - | macOS | brew |
| sag | sag | ELEVENLABS_API_KEY | 전체 | brew |
| session-logs | jq, rg | - | 전체 | 내장 |
| sherpa-onnx-tts | - | SHERPA_ONNX_* | 전체 | download |
| skill-creator | - | - | 전체 | 내장 |
| slack | - | - | 전체 | config |
| songsee | songsee | - | 전체 | brew |
| sonoscli | sonos | - | 전체 | go |
| spotify-player | spogo/spotify_player | - | 전체 | brew |
| summarize | summarize | - | 전체 | brew |
| things-mac | things | - | macOS | go |
| tmux | tmux | - | macOS/Linux | 내장 |
| trello | jq | TRELLO_API_KEY/TOKEN | 전체 | API |
| video-frames | ffmpeg | - | 전체 | brew |
| voice-call | - | - | 전체 | config |
| wacli | wacli | - | 전체 | brew/go |
| weather | curl | - | 전체 | 내장 |
